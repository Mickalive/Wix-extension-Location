{
  "lane": "integration",
  "task": "INT-C7-R1: Repair the deterministic gate failure on the integrated product tree at HEAD ec916b7. Run the full deterministic gate (npm run check = tsc --noEmit + purity gate + vitest; npm run build) on the current accepted state, identify every failing step, and fix the root cause within integration-lane scope (src/platform/**, tests/platform/**, package.json, tsconfig.json, vitest.config.ts, extensions.ts, build config). Do not modify governance files (.github/**, .opencode/**, AGENTS.md, MAIN_PROMPT.md, docs/NEXT_CYCLE.*, docs/PRODUCT_GATES.json). If the gate passes cleanly on the current tree with no product-code changes needed, produce a minimal no-op integration candidate confirming green deterministic checks so the gate evidence is persisted.",
  "reason": "The unresolved context states the fresh integrated deterministic gate failed. The deterministic gate (npm run check + npm run build) is the mandatory quality barrier before any product state can be accepted or promoted. All four lanes' prior audit work (Rules, Dashboard, Billing complete; Integration INT-C6-R1 accepted) is blocked until the gate is green. Rules, Dashboard, and Billing have no remaining product tasks (explicitly complete with evidence). Integration is the sole active lane and owns the build configuration and platform code that the gate validates. Every recent factory run has been assigned to integration and failed with transient OX provider errors (exit 75, final_auditor_unavailable_or_failed), meaning no integration candidate has been successfully audited since cycle 32920420147. The task must repair the gate so the factory can resume the integration→audit→integrate cycle."
}
