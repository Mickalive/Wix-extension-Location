{
  "lane": "dashboard",
  "task": "Same-lane repair of dashboard save/apply contract: (1) bridge.saveRuleSet must unwrap the { ruleSet, savedBy } envelope so handleSave receives the ruleSet directly, not the raw platform response; (2) bridge.requestApply must reject on HTTP 404 instead of resolving null, so handleApply surfaces missing-confirmed-plan as an explicit error rather than a misleading generic message; (3) implement a first-save convention: when state.savedRuleSet is null/undefined, omit expectedRevision from the PUT body or send a well-known sentinel that the platform accepts for create-vs-update, and add a faithful unit test proving the first-save path succeeds end-to-end; (4) gate the Apply affordance (button + keyboard shortcut) behind a check that a ConfirmedPlanLookup producer exists, or wire the producer if the integration lane can supply the confirm endpoint, so the apply flow does not silently promise an impossible operation; (5) upgrade all page-level test doubles (applyFlow.test.js, rulesEditorPage.test.js, rulesEditorEntitlement.test.js) from the raw-draft-returning fakes to the faithful shim that enforces envelope unwrapping, expectedRevision validation, and 404-as-error, eliminating the fidelity gap that hid F2 and F3. The accepted base is ec916b75d5600e02d679d264648ac92333d721f1. Per workflow: REJECT means rebuild from accepted state, not blindly patch the rejected candidate.",
  "reason": "Integrated audit REJECT on dashboard candidate d475f3d8. The candidate's own lane suite is RED (213/215 pass, 2 fail), and four critical findings (F1-F4) plus one medium finding (F5) all belong to dashboard lane ownership: bridge.js envelope handling (F1/F3), rulesEditorPage.js save/apply flow (F2/F3/F4), and test-double fidelity (F5). F2 makes first-save impossible (platform rejects empty expectedRevision with 400). F3 means any successful save crashes the render (envelope unpacked as ruleSet). F4 means apply always fails 404 (no ConfirmedPlanLookup producer). These are hard blockers that prevent the product from persisting a rule set or applying any change. Same-lane repair is mandatory before re-audit; no other lane's work is affected."
}
