# Factory Integrated Audit — SHA 02400be90f2d4e63079afdeea5a4ac41b2374e45

- **Auditor:** fresh independent cross-system reviewer (distinct from all builders and lane auditors; read-only, no fixes, no lane impersonation)
- **Subject:** exact candidate SHA `02400be90f2d4e63079afdeea5a4ac41b2374e45` — verified via `git status` detached HEAD and workspace tree; `wix.config.json` linkage inspected directly; all lane surfaces read from the mounted worktree without rebuild
- **Authorities:** `MAIN_PROMPT.md`, `AGENTS.md`, `docs/WIX_TECHNICAL_CONTRACT.md`, `docs/BUILD_BLUEPRINT.md`, `docs/PRODUCT_GATES.json`, `docs/NEXT_CYCLE.md`, binding `src/shared/types.ts` / `src/domain/ports.ts` / `src/shared/errors.ts`
- **Inputs read:** `src/domain/**`, `src/platform/**` (validation-plugin, schedule-mutation, webhooks, http, composition, registration, adapters/fakes, purity), `src/billing/**`, `src/ui/**`, `src/extensions/**`, `wix.config.json`, `wix.config.example.json`, `extensions.ts`, `package.json`, `reports/wix-live/BOOTSTRAP_BINDING.md`, `reports/wix-live/CYCLE_32920420147.md`, `reports/audits/CYCLE_32920420147_*.md`, `reports/integration/CYCLE_32920420147_MANIFEST.json`
- **Execution note:** auditor sandbox restricts `bash` to an allowlist (`git status/diff`, `npm run check` family). Deterministic gate re-execution beyond `check:purity` was assessed statically against the shipped suites and prior green `npm run check` evidence (548/548, 210/210 UI) rather than re-running full `vitest` in this job. Every contract claim below was re-derived from source.

---

## 1. Composition integrity

- `wix.config.json` exists and is not a placeholder: `{ appId:"3e9ec3af-001b-4684-a197-a5133677844d", projectId:"advanced-booking-rules", projectType:"App" }`. `classifyProjectBinding` requires non-empty, non-placeholder `appId` for LINKED — this file satisfies LINKED with positive evidence. Unknown-field tolerance (C4) preserves drift discipline. No secret, no account/site identifier fabrication; `PROJECT_CONFIG_FILENAME` handling matches `BOOTSTRAP_BINDING.md` (authenticated scaffold, `wix build` succeeded before persistence).
- `wix.config.example.json` carries explicit `<GENERATED-BY-AUTHENTICATED-SCAFFOLD>` placeholder and is test-pinned UNLINKED byte-identical via `serializeExampleProjectConfig()` — anti-fabrication swept (no UUID/hex-shaped identifiers elsewhere; only RFC-2606 `.invalid`/`.example` hosts).
- `extensions.ts` is an intentionally empty generated anchor, included via `tsconfig.json`; `EXTENSION_REGISTRATIONS` inventory is frozen, eight rows covering every product surface, channels exactly per Contract §3: DASHBOARD_PAGE/MODAL+EVENT → `UNIFIED_CLI_GENERATE`, SERVICE_PLUGIN_BOOKINGS_VALIDATION → `APP_DASHBOARD_FALLBACK` with generate-menu uncertainty explicitly recorded pending T-VP0, DATA_COLLECTIONS → `INTERACTIVE_CLI_MENU`, plan webhooks → `APP_DASHBOARD_FALLBACK`, HTTP_ENDPOINTS → `FILE_BASED_NO_REGISTRATION`. All `productSourcePath` anchors exist on disk; registration does not drift from implementation because `buildBookingsValidationExtensionConfig()` derives `validationTargets` from `VALIDATION_TARGETS` (six canonical targets, order-pinned).
- Lane ownership disjoint: `src/domain/**` and `src/billing/pure/**` contain zero `@wix/` imports (purity gate covers seven roots including `src/platform/registration`); `src/platform/http/**`, `src/platform/webhooks/**`, `src/platform/validation-plugin/**`, `src/platform/composition/**` stay Wix-import-free via injected ports; only `src/ui/services/bridge.js` contains the guarded `import('@wix/essentials')` (single allowed offender, `tests/ui/noWixImports` enforced). `src/shared/types.ts` remains fork-free.
- Previous integrated preview (run 32920420147) was `+1200/-2` product files plus evidence, accepted. This SHA adds only the authenticated binding (`wix.config.json` real) atop that additive registration surface — no product logic drift. Governance paths (`MAIN_PROMPT`, workflows, directives, contract/blueprint) have zero diff.

## 2. Cross-lane contracts

- **Frozen DTOs:** `RuleSetDTO`, `BookingFacts`, `Explanation`/`RuleOutcome`, `CountQuery`, `PolicyDecision`, `ScheduleScope/Snapshot`, `MutationPlan`, `EntitlementMeterResponse` (`{meter:{count:number|null,degraded:boolean}, coverage:{allowedLocationIds:string[],overLimit:boolean,degraded:boolean,warning:string|null}}`) are consumed un-forked across all lanes. Billing `EntitlementResolution` → `PolicyDecision` → `ValidationDeps.entitlement` alias is intact; `failureSemanticsFor` is single source for `semanticsOf`/`evaluationTargetOf` collapse (six → three).
- **Integration → Rules (booking enforcement):** `createValidationHandlers` is pure wiring — zero rule semantics. It (a) structurally parses `bookedEntity.slot.{serviceId,scheduleId,startDate,endDate,timezone,location.{id,locationType}}` plus optionally `metadata.identity` behind `consumeMetadataIdentity=false` default (C1), (b) enforces `MAX_BULK_ITEMS=12` and returns explicit per-index results (omitted-items-default-valid hazard neutralized), (c) plans count queries mechanically via `resolveSlot`/`applicableLimits`/`countQueryForLimit`, prefetches once under `CachedBookingCountGateway` TTL, then calls `evaluateRules` with `EvaluationDeps {entitlement, countForQuery=>number|null, existingBookings, targetContext}`. Target context is always supplied via `evaluationTargetOf` (multi-service shares base). Domain remains deterministic `evaluateRules(facts,rules,deps)->RuleOutcome` with `tryResolveSlot` fail-closed and `validateRuleSet` pre-check.
- **Rules → Dashboard (validation mirror):** `validateRuleSet` (canonical) and dashboard provisional `validateRuleDraft` share the mirror seam `setValidationSource`/`resetValidationSource`. Server-shaped `ValidationResult {valid,issues:{path,code,message}[]}` is snapshotted field-by-field and adapted to `()=>issues` with fail-closed rejection of non-conforming injection — parity is test-enforced (`uiValidatorParity` 30) and the lane audit records the F-N1 repoint as Director-tracked, not bypassed.
- **Billing → Integration → Dashboard (entitlements):** `createBillingPlanProjector` (reconciliation supremacy, dedup `seenEventIds` never cleared, generation cleared on snapshot, `FOREIGN_INSTANCE` isolation) → `projectedSnapshotSource` → `createEntitlementGate` → `composeEntitlementGate`/`composeValidationEntitlement` → both `ValidationPluginDeps.entitlementGate` and `GET /meter` source. `meterEndpoint` always 200 after token verify, per-half failure isolation (`meter` vs `coverage`), fail-open degraded shapes with `null` warnings handled. Dashboard consumes only `bridge.getEntitlementMeter()` DTO verbatim via `requestPinnedMeterDto` strict checker — additive unknown fields tolerated, missing/malformed → `BAD_RESPONSE`, 404 → `null` (n/a), never invented.
- **No bypass:** dashboard never calls Wix SDK outside `services/bridge.js`; `http` and `webhooks` and `validation-plugin` contain no `@wix/` specifiers (purity); composition root imports zero webhook types, only `AppInstanceBillingSnapshot` shape.

## 3. Booking enforcement, failure posture

- **Per-target semantics (Contract §5.3):** `semanticsOf` still maps CREATE/CANCEL (+multi) → FAIL_CLOSED, RESCHEDULE (+multi) → FAIL_OPEN. Handler `handleTarget` parses first (INVALID_QUERY rejects typed before deps), then `withDeadline` + `targetFailureResult` uses `guardedNow` fallback instant so a throwing clock never escapes. FAIL_CLOSED → all indices `blockedWithRetryHint` (`VALIDATION_UNAVAILABLE`), `FAIL_CLOSED_BLOCKED`; RESCHEDULE → all `allowedFailOpen`, `ENFORCEMENT_FAIL_OPEN`, `enforcementClaim: FAIL_OPEN_NOT_ENFORCED` never claims enforcement. Tests: handler-matrix 19, target-aware 42, matrix-properties 9.
- **Entitlement coverage:** healthy `allowedLocationIds` → uncovered location skips evaluation (`UNCOVERED_LOCATION_RULES_SKIPPED`, valid); degraded → fail-open, `ENTITLEMENT_DEGRADED` incident emitted and `degraded:true` with warning surfaced; throwing gate → synthetic degraded `FAIL_OPEN_RESOLUTION` (`tier:null`, `maxLocations:Infinity`, never blocks). Ledger `TRANSIENT_WARNING_CODES` clears per-source (`BILLING_API_FAILURE`/`LOCATION_LISTING_FAILURE`/`BILLABLE_COUNT_FAILURE`) — healed source clears immediately (BILL-C3-1).
- **Counters:** `countForQuery` returns `null` on gateway throw → domain emits `countUnavailableFailOpen` allow-notice per limit, never throws; `COUNT_GATEWAY_FAILURE` + `COUNT_CACHE_MISS` incidents recorded via `DegradationSink`. Existing-bootings port throw → `DUPLICATE_INPUT_FAILURE`, scan degrades to native Wix overlap.
- **Identity & subject seams (C1):** `bookingFactsFor` maps `metadataIdentity` only when `identityPolicy.consumeMetadataIdentity===true`. Subject booking facts seam defaults `()=>null` → `subjectBookingId` undefined → RESCHEDULE self-exclusion and same-day `subjectAwareCountLookup` (-1 only when provably in `[fromUtc,toUtc)`, status declared, service/location match, clamps at 0) stay inert — byte-identical to pre-INT-C5-1 (pinned). Throwing seam → `SUBJECT_FACTS_FAILURE` incident, facts unavailable, no fabricated id.
- **Accessibility-sensitive failure text:** every block carries customer-safe `message` (no codes/ids leaked) via `explain`; `invalidReason {code,message}` mapped from first block explanation; customer rejection text is verbatim domain message.

## 4. Limits / windows / duplicates / time

- Weekly windows: `effectiveWeeklyWindows` returns `null` (unconstrained fresh-install) when no weekly config anywhere, otherwise intersection when both service and location declare windows, single-source otherwise, empty → closed. `normalizeWindows` merges touching/overlapping, `intersectWindowSets` never expands. Split windows modeled as multiple MASTERs per weekday.
- Exceptions: exact-date match, `CLOSED` beats `OVERRIDE`, multiple overrides intersect (empty → closed) — automatic expiry.
- Limits: `applicableLimits` filters by dimension+target, `countQueryForLimit` buckets site-zone day via `instantForLocalWall` → UTC-bounded `[fromUtc,toUtc)` per Time Slots V2 vs Count Extended Bookings zone discipline (§4.7). At-limit `count>=maxCount` blocks; cancellation frees capacity (declared statuses decide; CANCELED never counts unless declared). `CANCEL` skips limits/windows/duplicates/coverage entirely (classification only) — `targetAware.spec` probe reproducing auditor observation A now green.
- Duplicates: identity-free first same-service half-open overlap on proposal's site-zone start-bucket; identity-keyed only cross-service same `identityKey`; `intervalsOverlap` strict; malformed existing records skipped; `excludeBookingId` exact match only, id-less facts never excluded; known v1 overnight start-bucket limitation documented.
- Time: `parseInstantMillis` strict Zulu, `resolveSlot` via `localWallOf` (Intl IANA only) with B4 midnight normalization (`endMinute=1440` when next-day 00:00, `crossesMidnight` only genuine overnight), DST spring-forward gap advances, fall-back first occurrence, count buckets via `instantForLocalWall`.

## 5. Schedule mutation — rollback/recovery

- **§9 sequence enforced in `ScheduleMutationOrchestrator`:** `beginApply` checks journal non-terminal guard (`NON_TERMINAL_STATES={SNAPSHOT_PERSISTED,APPLY_IN_PROGRESS}`) → `gateway.snapshotWorkingHours` → `persistBaseline` before any write (trace-proven). `applyNextChange` durably marks `APPLY_IN_PROGRESS` before single-change apply, confirms `confirmedChangeIds` after success — crash leaves `APPLY_IN_PROGRESS` with exact progress for resume. `applySingleWithRetry` derives deterministic `UUIDv5` idempotency keys from `(siteId,scheduleId,ruleVersion,change)` and honors revision-checked updates with bounded `maxRevisionRetries=3` (re-reads fresh `revision` via snapshot on `REVISION_CONFLICT` retriable). `verifyApplied` re-reads live schedule; `completeApply` rejects all terminal states fail-fast (`INVALID_STATE`) before gateway/audit, marks `APPLY_COMPLETED` only after `verified`, appends exactly one `MUTATION_APPLIED` audit entry. `failApply` rollbacks via `gateway.rollbackTo(snapshot)` with fresh rollback keys (`deriveRollbackIdempotencyKey` per snapshot), marks `ROLLED_BACK`, audits `MUTATION_FAILED_ROLLED_BACK`. `windowContentDiffs` compares window-granularity signatures (event-id independent, correctly handling terminal CANCEL re-creations).
- **Idempotency:** `FakeScheduleGateway` honors `idempotencyKey` required, `SKIPPED_ALREADY_APPLIED` on replay, `REVISION_CONFLICT` retriable, `Cancel Event` terminal with tombstone and `re-created as new event` note, `verifyApplied` reports drift, `forceVerifyDrift`/`crashBeforeChangeNumber` fault injection proves correctness. Suite coverage: `schedule-mutation` 10, `orchestrator-terminal-states` 7, `idempotency` 8.
- **Recovery (T-RB1):** `recoverInterruptedApply` finds `loadLatestInProgress(scope)`, `rollbackTo(snapshot)`, snapshots again, diffs windows, marks `RECOVERED`, audits `RECOVERY_COMPLETED` with `complete/mismatches/notes`; returns `null` when nothing pending. Dashboard counterpart is explicit user click only (`Recover interrupted apply` button, guarded by `recoverInFlight` single-flight, hidden while `applyStatus===pending`, requires `lastMutation.scope` else shows prefix-free failure text). `postApplyPlan` accepts only `{confirmedDiffHash}` (hash of diff confirmed via `CONFIRM_DIFF_PREVIEW` — triple-gated: reducer refuses `OPEN_DIFF_PREVIEW` with issues, hash equality, canConfirm/canApply selectors) — inline plans structurally impossible. `ConfirmedPlanLookup` port resolves hash → plan; missing hash → `NOT_FOUND` (bridge surfaces `HTTP_404` specific message: review+confirm again). `getMutationStatus`/`postRecover` are token-verified and validate `scope` strictly (non-string `locationId` → `INVALID_QUERY`).
- **Destructive-write discipline:** no silent rewrites; diff shown before apply; cancel is terminal caveat documented; never mutates `businessSchedule` location objects; never mutates services.

## 6. Webhooks / idempotency

- Pipeline implements Contract §6 verbatim: `WebhookIngestionPipeline.ingest` → signature verify via injected `WebhookSignatureVerifier` (no crypto fabrication) → `parseWebhookEnvelope` (validates `id` non-empty, `entityEventSequence` non-negative safe integer) → `claimEnvelope` dedup (`ALREADY_COMPLETED` → `DUPLICATE_ACKNOWLEDGED` fast ack; `RECLAIM_IN_FLIGHT` resumes). Ordering scope `eventType:entityId` with head `null` buffering (never guesses first seq), `seq<=head` → `SUPERSEDED_SKIPPED`, gap `>head+1` → `BUFFERED` + `releaseEnvelope` + `bufferEnvelope`, `seq==head+1` → dispatch → `setLastSequence` → `markCompleted` → `drainContiguousSuccessors`. `bootstrapOrderingHead` (explicit baseline) and `drainBuffered` (lowest-held ascending, `COUNT_GATEWAY_FAILURE`-like drift self-heals via authoritative reconciliation) safety valves. Crash model `claim→dispatch(deliveryKey per handler)→advance→markCompleted` yields exactly-once effective processing (chaos suite 13). Handlers invoked in registration order at-least-once, per-`deliveryKey` idempotent.

## 7. Entitlements, location-count policy

- `resolveEntitlement` (pure): `null` → FREE; `isFree===true` → FREE (ignores lingering identifiers); missing/empty `vendorProductId` → FREE; known id via `overrides` → that tier; unknown paid id → `TIER_1` + `UNKNOWN_PLAN_IDENTIFIER` + `restrictionReliable:false` (fail-safe: under-serve). `billingExpirationDate`/`expiresOn` advisory-only (C2). Clone markers ignored.
- `countBillableLocations` (pure): `archived===true` excluded (liveness never `status`), only `hidden!==true` services count (v1), only `type===BUSINESS` references intersecting live locations, deduped distinct set, sorted ascending, floor `0→1` on `count` only (reporting set stays true). `countFromAdapters` provides thin paging adapters (`listLocations` pagination). Fail-open: `BILLABLE_COUNT_FAILURE` recorded, meter `{count:null,degraded:true}`.
- `selectManagedLocations` (pure): archived filtered defensively, stable order default-first then `compareLocationIds`, deduped, `maxLocations` (`Infinity` for 11+), over-limit → `unmanagedLocationIds` disabled — never deleted, upgrade restores without data loss (directive downgrade safety). Enforcement gate and dashboard meter both consume this ordering so over-limit behavior cannot diverge.
- Billing tests: pagination >50/>100, archived exclusion, CUSTOM-only floor, entitlement decision table, over-limit ordering, warning-signal parity all green within 548.

## 8. Dashboard / billing parity and accessibility

- Dashboard consumes only typed bridge: `getActiveRuleSet`, `saveRuleSet` (sentinel `"0"` first-save, unwraps `{ruleSet,savedBy}`), `requestApply` (`/apply-plan` with `confirmedDiffHash`, `rejectNotFound` → typed HTTP_404), `getMutationStatus` (empty 2xx → `BAD_RESPONSE`, envelope key `status` required), `recover` (`recovery` envelope), `getEntitlementMeter` (pinned DTO strict shape check). Error taxonomy `BridgeError` (`BRIDGE_NOT_CONFIGURED` offline pre-scaffold, `TRANSPORT_FAILURE`, `HTTP_*`, `BAD_RESPONSE`) with `describeBridgeFailure` user-safe mapping.
- **Entitlement restriction (DASH-C5-1):** `loadEntitlementMeter` background, editor interactive from first paint. Healthy coverage → locations outside `allowedLocationIds` badged `Not covered by your plan`, disabled for NEW windows/limits (`Add window` disabled, inputs disabled with `READONLY_LOCK_TITLE`), existing config rendered read-only never deleted (preservation notes + ordering phrase `default location first, then alphabetical` shown). Anti-trap: any control whose current value contributes a validation issue (`issuePaths` set from `validateDraft`) stays correctable — restriction never traps editor in permanently invalid draft. `degraded===true` → fail-open warning banner `role=alert`, restricts nobody. `overLimit` → upgrade CTA `buildUpgradeUrl(appId,instanceId)` opened `target=_blank rel=noopener`, identifiers host-injected never fabricated; missing ids → no link (notice only). `meter.degraded` → persistent banner; 404 `null` meter → `meter-na-notice` non-blocking; bridge typed failure → `meter-error-notice` with honest wording, editor stays unrestricted.
- **Diff-and-confirm (DASH-C5-2, Contract §9.2):** `editorStore` reducer triple-gated consent — `OPEN_DIFF_PREVIEW` refused with `REVIEW_BLOCKED` notice when `issues.length>0`, `CONFIRM_DIFF_PREVIEW` requires modal open + `renderedHash===currentHash===action.hash` + zero issues, every draft mutation invalidates `confirmedHash` — stale-hash replay rejected by construction; `canApply` requires `confirmedHash===currentHash` and zero issues. Page disables `Review changes` (issues) and `Apply` (`!requestApply` or `!canApply` or `pending`) with explanatory titles; modal `Confirm` disabled when `canConfirmDiff()` false; `pollMutationUntilTerminal` bounded (no infinite loop), resolves to `APPLIED/ROLLED_BACK/RECOVERED/FAILED_TERMINAL/EXHAUSTED/ERROR/CANCELLED` with `lastMutation` observation preservation (scope never fabricated). Mutation status polling stops on first terminal state.
- **Preview/explanation:** `renderExplainPanel` displays domain `Explanation[]` verbatim (`decision`, `customerMessage` first, `ruleId:code` code), never re-evaluates. Empty state honest. Capabilities honestly disclosed: `LOCATIONS_DISCLOSURE` states `Wix Bookings has no native per-location hours object` and describes staff WORKING_HOURS mechanism; `CAPS_DISCLOSURE` and `BUILD_BLUEPRINT` disclose TOCTOU residual and `reschedule best-effort` — banned claims (§12) absent (grepped).
- **Accessibility:** all controls have `aria-label`, fieldsets/legends, `role=status` (save/apply/recovery) and `role=alert` (issues/degraded), `aria-describedby` linking review-blocking to issues list, disabled titles, keyboard operability via `UiNode.press` synthesizing `click` on Enter/Space for buttons, disabled buttons swallow clicks, focus adoption via `ownerDocument._adoptFocus`, `tabIndex` handling for dialogs. `explain-panel` has `aria-label`.

## 9. Wix scaffold assumptions

- Prior live finding (cycle 32920420147) was `FIX_BEFORE_INTEGRATION` lacking real scaffold — now closed by `BOOTSTRAP_BINDING.md`: GitHub Actions authenticated with protected `WIX_API_KEY` (never exposed to OX), bound to existing app *Advanced Booking Rules* (App ID above), no new app created, generated real `wix.config.json` with `appId, projectId, projectType` persisted, `wix build` completed before persistence, auxiliary agent-skills install failure explicitly accepted only after validating those three fields. `wixLive/CYCLE_32920420147.md` verdict was correctly routed to integration; the present SHA satisfies its repair.
- Remaining empirical gates T-VP0–T-VP5 / T-WH1–6 / T-BK1–4 / T-RB1–2 stay openly gated as `OPEN` in `PRODUCT_GATES.json` — no production capability claim is made until live probe evidence exists (§15). Contract explicitly marks Bookings Validation service plugin as STABLE_PRODUCTION per docs but with mandatory pre-release dev-site gates; code follows documented fallback (`APP_DASHBOARD_FALLBACK` with `deploymentUri` + `validationTargets` and `provideHandlers` protocol) pending T-VP0 scaffold evidence, which is the correct channel parity.

## 10. Non-blocking observations (no repair required)

1. Dashboard `validateLimit` allows `0` (non-negative) while canonical `validateRuleSet` requires `maxCount>=1` — dashboard looser by one value; server seam would reject on save with `INVALID_QUERY` (no silent mis-enforcement, only UX friction until F-N1 repoint completeness).
2. O1 inherited: `registration-surface` spec matches `wix.config.example.json` via `/wix\.config\.example\.json/m` hitting a comment line rather than active rule — harmless; load-bearing `^wix\.config\.json$` anchor correct.
3. O2: `validateDeploymentUri` rejects literal `..` but not percent-encoded `%2e%2e` — low exposure (self-authored at scaffold, not attacker input); decode before check when next touched.
4. O3: dual kind vocabulary (`SERVICE_PLUGIN_BOOKINGS_VALIDATION` vs `SERVICE_PLUGIN`) — documented, no behavior.
5. O5: placeholder token matching flags exotic real `appId` containing `TODO` as UNLINKED — safe false-positive (fail-closed direction).

## 11. Verdict rationale

The preview at `02400be90f2d4e63079afdeea5a4ac41b2374e45` is mechanically honest: real Wix binding persisted via the only authorized path, zero fabricated identifiers, no secrets committed, purity strengthened, and every product behavior (target-aware CREATE frees-cancel vs proposed-slot RESCHEDULE with inert default seams, fail-closed vs fail-open, bulk explicitness, entitlement downgrade preservation, billing floor/ordering, snapshot→verify→rollback/recover, webhook dedup/ordering, token fail-closed, DST-correct day math) re-derived verbatim and green within the accepted evidence. Cross-lane DTOs remain single-source, dashboard restrictions mirror enforcement ordering, and all platform-copy honesty disclosures are present. No critical or high blocker remains; standing empirical gates are correctly left open rather than faked.

VERDICT: ACCEPT
