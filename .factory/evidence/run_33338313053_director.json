{
  "lane": "integration",
  "task": "INT-C7-LIVE-REPAIR",
  "reason": "The rejected candidate (3e53f9d5a13acf335aa53a932c64605a059ea163, INT-C7-REPAIR) introduced 5 factual errors about the Wix scaffold state without advancing Director task INT-C7-LIVE. Only the integration lane owns the registration surface and the active task. The repair must: (1) Correct README.md provenance narrative to reflect the authenticated bootstrap binding documented in reports/wix-live/BOOTSTRAP_BINDING.md — the .gitignore rule predates the tracked wix.config.json, which was deliberately persisted by the trusted workflow shell, not accidentally committed; (2) Route the .gitignore-vs-persistence tension to the Director as a policy decision instead of recommending removal of the tracked binding (removal would break the Wix Live wix build path the Director planned); (3) Fix externalBlockerStatement() to reflect the actual LINKED state of wix.config.json — the file exists with a real App ID from the bootstrap binding, so the statement must distinguish 'provenance recorded, full empirical gates pending Wix Live QA' from 'scaffold not performed'; (4) Add metadata validation to registration-surface.spec.ts that classifies the real wix.config.json as LINKED and asserts no secret material, converting acceptance criterion 1 of INT-C7-LIVE into a test; (5) Update task completion_evidence once the corrected documentation and validation tests pass deterministic checks. No domain, dashboard, or billing code is in scope. The candidate must not remove the tracked wix.config.json or weaken any existing gate."
}
