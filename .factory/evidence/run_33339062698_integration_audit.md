# Integration Lane Audit — Candidate ca6b15ec263d2b76caee416e4adcd1e17b24f954

**Base:** ec916b75d5600e02d679d264648ac92333d721f1  
**Candidate:** ca6b15ec263d2b76caee416e4adcd1e17b24f954 (wix-integration-builder generation 66)  
**Auditor:** integration-auditor (adversarial, read-only)  
**Date:** 2026-08-30

---

## Scope

This audit covers the exact integration candidate diff against the accepted base. The candidate modifies the Wix CLI project-binding classifier and scaffold prerequisites surface under `src/platform/registration/` and corresponding tests.

Files changed:
- `src/platform/registration/index.ts` (+1 export)
- `src/platform/registration/projectConfig.ts` (+61/-2 lines)
- `src/platform/registration/scaffoldPrerequisites.ts` (+42/-1 lines)
- `tests/platform/registration-project-config.spec.ts` (+81/-1 lines)
- `tests/platform/registration-surface.spec.ts` (+24/-1 lines)

---

## 1. Scaffold Authenticity Verification

**Official scaffold evidence** (from `origin/main/.factory/evidence/run_33321707099_official_scaffold.json`):
- `source`: "authenticated official Wix existing-app scaffold"
- `appId`: "3e9ec3af-001b-4684-a197-a5133677844d"
- `projectId`: "advanced-booking-rules"
- `projectType`: "App" (uppercase, Wix-generated)
- `pristineWixBuild`: "PASS"
- `scaffoldPackageSha256`: "1768e7a61f8e81751712fe4d3abd985aeee9df012cc98b35c2d5dcaad97c98cd"
- `developmentSiteProvisioned`: true

**Pristine build log** (from `origin/main/.factory/evidence/run_33321707099_official_scaffold_pristine_build.txt`) confirms `wix build` completes successfully with the official scaffold.

**Candidate test** `tests/platform/registration-project-config.spec.ts` lines 197–211 now includes a test that classifies this exact bootstrap-bound real config as `LINKED` with the documented App ID and no warnings (since `projectType: 'App'` is a known value).

**Finding:** The candidate correctly recognizes and validates the official scaffold provenance. The real `wix.config.json` generated by the authenticated one-time scaffold (`npm create @wix/new@latest app`) contains `projectType: 'App'` (uppercase) and a real `appId`. The classifier accepts this without warnings.

---

## 2. Anti-Fabrication Guarantees

### 2.1 Project Binding Classifier (`projectConfig.ts`)

**Positive evidence requirement for LINKED:** The classifier grants `LINKED` status **only** when:
- File exists and is valid JSON object
- `appId` field is present, a string, non-empty, and **not** a scaffold placeholder (detected via `looksLikeScaffoldPlaceholder`)

**Placeholder detection** (lines 50–78): Comprehensive — catches empty strings, angle-bracket `<...>`, double-brace `{{...}}`, template `${...}` shapes, and marker tokens (`GENERATED-BY`, `REPLACE`, `PLACEHOLDER`, `TODO`, `TBD`, `YOUR_`) case-insensitively.

**Drift tolerance (Contract §11 C4 analog):** Unknown extra fields are preserved in `RawProjectConfig` and never rejected. The committed `wix.config.example.json` (with `projectType: 'app'` and placeholder `appId`) classifies as `UNLINKED` by construction — test-enforced byte-equality + classification pin.

**projectType validation (INT-C7-LIVE addition):**
- Known values: `'app'` (documented example) and `'App'` (Wix-generated, per bootstrap evidence)
- Unknown values are **tolerated with a warning** (non-fatal, recorded in `warnings` array on `LINKED` result)
- Optional `options.validProjectTypes` override for stricter validation contexts

**No fabrication:** Nothing generates, defaults, or invents identifiers. The `LINKED` verdict requires a real-looking `appId` string.

### 2.2 Scaffold Prerequisites (`scaffoldPrerequisites.ts`)

**State-aware status statement:** New `scaffoldStatusStatement(linked: boolean)` produces truthful output in both directions:
- `linked=false`: Narrow external prerequisite statement (identical to deprecated `externalBlockerStatement()`), identifier-free, references Contract §16/T-VP0/runbook
- `linked=true`: Confirms real binding exists, notes remaining prerequisites (dev-site consent, extension registration, release), no identifiers exposed

**Prerequisites record:** All 5 entries have `owner: 'HUMAN_ACCOUNT_OWNER'`, explicit `whyNotDerivableInCi`, evidence gate references (T-VP0/Contract §16), and existing runbook anchors (`docs/runbooks/T_VP0_SCAFFOLD.md`).

### 2.3 File Boundary Protection

`.gitignore` line 19: `wix.config.json` is explicitly gitignored with explanatory comments referencing the authenticated scaffold and the example template.

**Test enforcement:** `registration-surface.spec.ts` line 210–213 verifies the gitignore rule exists.

---

## 3. Test Coverage & Deterministic Reproduction

**All 557 tests pass** (including 20 in `registration-project-config.spec.ts` and 19 in `registration-surface.spec.ts`).

**Key test pins reproduced:**
- `MISSING_FILE` / `UNPARSEABLE` / `UNLINKED` / `LINKED` are mutually exclusive
- Placeholder-shaped `appId` values → `UNLINKED` (never "linked")
- Real `appId` + known `projectType` (`'app'` or `'App'`) → `LINKED` without warnings
- Real `appId` + unknown `projectType` → `LINKED` with warning (C4 drift discipline)
- Custom `validProjectTypes` option overrides built-in known values
- Committed `wix.config.example.json` → byte-identical to module serialization → classifies `UNLINKED`
- Bootstrap-bound real config (`appId: '3e9ec3af-001b-4684-a197-a5133677844d', projectType: 'App'`) → `LINKED`, no warnings
- `scaffoldStatusStatement(false)` = `externalBlockerStatement()` (backward compatibility)
- `scaffoldStatusStatement(true)` produces linked message without identifiers
- No UUID-like or SDK import shapes in registration surface files
- Purity gate passes: `src/platform/registration` protected, no `@wix/` imports

---

## 4. Contract & Blueprint Alignment

| Contract/Blueprint Requirement | Candidate Status |
|---|---|
| Technical Contract §13 UQ4: `wix.config.json` field set honesty | **Met** — classifier requires only `appId`; tolerates unknown fields; validates `projectType` against known values from bootstrap evidence |
| Technical Contract §16: Human-owned prerequisites recorded | **Met** — 5 prerequisites, all `HUMAN_ACCOUNT_OWNER`, runbook-anchored |
| Technical Contract §15: Empirical gate T-VP0 | **Met** — scaffold prerequisites feed T-VP0; status statement reflects live-QA disposition |
| Blueprint §1: `src/platform/registration/` ownership | **Met** — integration lane owns this surface |
| Blueprint §5: Error model — fail-closed on binding failure | **Met** — `UNLINKED` with explicit problems; never invents `LINKED` |
| Blueprint §8: MVP scope fence — credential-free value | **Met** — all code pure, no Wix SDK imports, tests run credential-free |

---

## 5. Adversarial Checks Performed

1. **Attempted to fabricate a linked config** — classifier correctly rejects placeholder `appId` values (empty, `<GENERATED-BY-AUTHENTICATED-SCAFFOLD>`, `{{APP_ID}}`, `${APP_ID}`, `REPLACE_ME`, `your_app_id_here`, `TODO`)

2. **Attempted to bypass projectType validation** — unknown values produce warnings but do not block `LINKED` (correct per C4 drift discipline); custom `validProjectTypes` option works as escape hatch

3. **Checked for identifier leakage** — `scaffoldStatusStatement(true)` contains no UUIDs, app IDs, or project IDs; only references to the scaffold command and contract sections

4. **Verified example template drift detection** — test enforces byte-equality between committed `wix.config.example.json` and `serializeExampleProjectConfig()`

5. **Purity gate** — registration directory remains free of `@wix/` imports (CI-enforced)

---

## 6. Findings

**No blocking defects found.** The candidate:
- Correctly implements the INT-C7-LIVE enhancements to the project-binding classifier
- Adds state-aware scaffold status reporting for live-QA truthfulness
- Maintains all anti-fabrication guarantees (positive evidence for `LINKED`, placeholder rejection, drift tolerance)
- Aligns with official scaffold evidence (bootstrap `projectType: 'App'`, real `appId`)
- Passes all deterministic tests including new coverage for projectType validation and scaffold status statements
- Respects lane boundaries (integration lane only)
- Introduces no credentials, fabricated identifiers, or unsupported Wix claims

---

## Verdict

VERDICT: ACCEPT