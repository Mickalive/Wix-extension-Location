{
  "lane": "integration",
  "task": "INT-C8-REPAIR-GATE: Diagnose and repair the deterministic gate failure on the integrated tree at ec916b75d5600e02d679d264648ac92333d721f1. Run npm run check and identify the failing step (tsc, purity, or vitest). Fix the root cause within the integration lane's allowed scope (src/platform/**, tests/platform/**, package.json, tsconfig.json, extensions.ts). Rules, Dashboard, and Billing are complete — do not cross lanes. If the failure is caused by a cross-lane incompatibility, surface the exact evidence and stop rather than patching outside scope. Acceptance criteria: npm ci && npm run check && npm run check:offline all exit 0 with zero regressions against the 548-test baseline; purity gate green over all seven roots; no governance, domain, billing, or UI files modified.",
  "reason": "Fresh integrated deterministic gate failed at ec916b75d5600e02d679d264648ac92333d721f1. The only active lane in the last cycle was integration (INT-C7-LIVE for real Wix binding). Rules/Dashboard/Billing are all complete with no pending repair queues. The deterministic gate (tsc + purity + vitest) runs over the full integrated tree and is the integration lane's composition responsibility. A failure here blocks every subsequent cycle and must be repaired before any other lane can be dispatched. The prior cycle 7 integrated audit accepted 548/548 tests, so the regression was introduced between that accepted state and the current HEAD — squarely in integration's composition or scaffold surface."
}
