{
  "lane": "integration",
  "task": "INT-C7-REPAIR-F1-FIX: Replace the InMemoryConfirmedPlanStore with a durable storage mechanism so the confirm→apply flow survives across Wix serverless invocations. The current in-memory Map assumes single-instance execution, which contradicts the binding recon evidence (Wix-managed serverless with automatic scaling). Repair must persist the confirmed plan record so that POST /apply-plan can retrieve it from a different cold-started instance. Additionally harden by: (F3) recomputing the confirmation hash server-side from the canonical plan serialization and rejecting mismatches, (F5) deriving confirmedBy from the verified caller token instead of trusting the client. F4 (confirm-diff validation) may be addressed opportunistically. The repair must preserve the existing 31 passing confirm-diff/apply-plan tests, add tests for cross-invocation durability and hash-integrity rejection, and maintain deterministic checks green.",
  "reason": "The integrated cross-lane audit of SHA b8ac3832f355adc631fa30db2182515e225a5535 returned FIX_BEFORE_INTEGRATION. Finding F1 is a HIGH-severity load-bearing reliability defect: InMemoryConfirmedPlanStore is an app-lifetime-scoped Map, but Wix serverless cold-start semantics (documented in reports/recon/PLATFORM.md and src/platform/composition/README.md) mean the confirm and apply HTTP requests routinely land on different instances, causing apply-plan to return 404 NOT_FOUND immediately after the user confirmed. This breaks the primary mutation flow in the common case. F1 is within integration-lane scope and must be repaired before the preview is adopted. Findings F3 and F5 are hardening items owned by integration in the same repair scope. Finding F2 (dashboard bridge missing confirmDiff method) is pre-existing, correctly documented by the candidate, and must be routed to the dashboard lane in the same or next cycle but does not block this integration repair. State: cycle 21, last_result NOT_READY (final auditor unavailable); the integration lane repair is the highest-priority product-advancing work available."
}
