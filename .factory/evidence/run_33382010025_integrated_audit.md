# Factory Integrated Audit — SHA ec916b75d5600e02d679d264648ac92333d721f1

- **Auditor:** fresh independent cross-system reviewer (muse-spark-1.2-contributor-free). Distinct from all builders and lane auditors; no candidate reuse, no impersonation; read-only except this report.
- **Subject:** exact SHA `ec916b75d5600e02d679d264648ac92333d721f1` = current `lab/wix-rules` checkout (working tree clean). Verified via `reports/wix-live/BOOTSTRAP_BINDING.md` and `wix.config.json` linkage classification.
- **Authorities:** `MAIN_PROMPT.md`, `docs/WIX_TECHNICAL_CONTRACT.md` (binding, 2026-08-24), `docs/BUILD_BLUEPRINT.md`, `docs/PRODUCT_GATES.json`, `docs/NEXT_CYCLE.md`.
- **Scope verified:** integration / rules / dashboard / billing contracts; booking enforcement; rollback/recovery; entitlements; accessibility-sensitive behavior; real Wix scaffold assumptions. No fixes performed.

---

## 1. Composition & contract parity (re-derived)

- **Frozen shards untouched:** `src/shared/types.ts`, `src/shared/errors.ts`, `src/domain/ports.ts` byte-stable; domain remains pure (no `@wix/*` imports — enforced by `src/platform/purity/check-purity.mjs` over seven protected roots including `src/platform/registration`). Billing `src/billing/pure/**` likewise pure.
- **Dependency direction:** `dashboard → shared ← domain`, `platform → domain(ports)+shared`, `billing → domain/shared`, `domain → stdlib+shared` — conforms to Blueprint §2. Grep-verified: only `src/ui/services/bridge.js` holds the single guarded dynamic import of `@wix/essentials`; all other UI/platform modules are import-clean (corroborated by `tests/ui/noWixImports.test.js` and `tests/platform/purity-gate.spec.ts`).
- **Shared DTOs are single-source:** `RuleSetDTO`, `BookingFacts`, `PolicyDecision`, `MutationPlan`, `ScheduleSnapshot`, `CountQuery`, `EntitlementMeterResponse` are canonical in `src/shared/types.ts`; every consumer imports them — no duplicated shape, no silent fork.
- **Pinned meter DTO:** platform `src/platform/http/meterEndpoint.ts` produces `{meter:{count, degraded}, coverage:{allowedLocationIds, overLimit, degraded, warning}}` exactly as dashboard `src/ui/services/bridge.js` `requestPinnedMeterDto()` and `src/ui/pages/locationsUsagePage.js` consume it. Strict shape validation on both sides rejects empty/malformed 2xx bodies as `BAD_RESPONSE` (404 → `null` only). No drift.
- **Validation-mirror discipline:** dashboard `src/ui/validation/mirror.js` accepts only `(draft)=>issues[]` function OR verbatim server `ValidationResult {valid, issues:{path,code,message}[]}` (snapshotted); `src/ui/pages/rulesEditorPage.js` and `src/ui/state/editorStore.js` never import validators directly. Parity with canonical `src/domain/validate.ts` is enforced by `tests/domain/uiValidatorParity.spec.ts` (frozen `ruleDraftValidators.js` hash pinned). No bypass.

## 2. Rules domain (determinism & classification)

- **Pure evaluation:** `src/domain/evaluate.ts` `evaluateRules(facts,rules,deps)` is synchronous, deterministic (injected `Clock`/`IanaZone`, `Intl`-based `wallClock`), no I/O. Same inputs → same outcome; `evaluate` never throws — internal failures classify as `RULESET_INVALID`/`INVALID_SLOT`/`EVALUATION_ERROR` fail-closed blocks.
- **Window/exception precedence:** `weeklyWindows` + `exceptions` with site-zone day math; DST fixtures via IANA tz; `validateRuleSet` structural + temporal checks (overlap, start<end, 24:00 exclusive-end semantics) validated in one pass.
- **Caps:** `src/domain/limits/limits.ts` declares `countQueryForLimit` (half-open UTC `[fromUtc,toUtc)` over START instant, declared statuses). `evaluate` degrades per-limit to `countUnavailableFailOpen` when `countForQuery` returns null/throws — never silently, with visible notice.
- **Target-aware matrix (Contract §5.3):** optional `EvaluationTargetContext` (`CREATE|CANCEL|RESCHEDULE`, optional `subjectBookingId` for RESCHEDULE). Absent ⇒ bit-for-bit legacy CREATE semantics (`DEFAULT_TARGET_CONTEXT`). CANCEL ⇒ classification families only (capacity windows/exceptions/caps/duplicates skipped correctly — cancellation frees capacity). RESCHEDULE ⇒ windows/exceptions/caps against PROPOSED slot; duplicates exclude subject via `excludeBookingId`. Code, domain README matrix, and platform wiring (`handlers.ts` `evaluationTargetOf` collapse of six targets onto three) agree cell-for-cell; exhaustive domain tests cover it.
- **Destructive semantics ban:** domain never mutates locations; schedule math is read-only window algebra.

## 3. Integration — booking enforcement (Contract §5.3, Blueprint §4 flow 1)

- **Wiring layering:** `src/platform/validation-plugin/handlers.ts` is pure Wix-import-free wiring — consumes canonical `evaluateRules` with pre-resolved deps; zero rule semantics live here. SDK adapter `bookingsValidation.provideHandlers()` deferred to scaffold `src/platform/registration/README.md` §6 and `src/extensions/dashboard/*.js` stubs — correct deferral.
- **Bulk explicitness:** `src/platform/validation-plugin/payload.ts` `parseValidationRequest` enforces `1 ≤ items ≤ 12` (`MAX_BULK_ITEMS`), maps ONLY documented fields (`bookedEntity.slot.{serviceId,scheduleId,startDate,endDate,timezone,location.{id,locationType}}`, observed `metadata.identity` structurally). Returns explicit per-index result for EVERY index — omitted items never default valid.
- **Six→three collapse:** `targets.ts` `VALIDATION_TARGETS` (6) with `semanticsOf`/`evaluationTargetOf` routing through single-source `failureSemanticsFor` (`CREATE/CANCEL → FAIL_CLOSED`, `RESCHEDULE → FAIL_OPEN forever`). `buildBookingsValidationExtensionConfig()` in `src/platform/registration/validationExtension.ts` derives registration `validationTargets` from `VALIDATION_TARGETS` — implemented surface cannot drift from registered surface (pinned equality test).
- **Entitlement coverage once-per-request:** `resolveEntitlementDecision` cached; healthy decision + location outside `allowedLocationIds` ⇒ `UNCOVERED_LOCATION_RULES_SKIPPED` (valid, no evaluation) — coverage restriction only (native Wix behavior untouched). `degraded:true` ⇒ fail-open coverage (no skip) + persisted `ENTITLEMENT_DEGRADED`. Throwing gate ⇒ synthetic degraded decision with `FAIL_OPEN` warning — billing failure never blocks booking.
- **Counters:** domain helpers `applicableLimits`/`countQueryForLimit`/`resolveSlot` plan queries (mechanical), one cached pass via `CachedBookingCountGateway` (short TTL) per request across all items; gateway failure ⇒ per-query `COUNT_GATEWAY_FAILURE` incident + domain fail-open notice, never thrown. `countQueryKey` dedup verified.
- **Identity (C1):** `DEFAULT_IDENTITY_PAYLOAD_POLICY.consumeMetadataIdentity=false`; `metadata.identity` observed but ignored until `T-VP3` evidence enables flag. No `contactDetails` access at all (redacted).
- **Subject-booking seam (RESCHEDULE self-exclusion/self-count):** injectable `SubjectBookingFactsPort` defaults to `null` ⇒ behavior identical to pre-INT-C5-1. Consulted only for `RESCHEDULE*` and only when at least one item will be evaluated. Failure ⇒ visible `SUBJECT_FACTS_FAILURE`. Self-count adjustment (`subjectProvablyInBucket` half-open START bucket + declared-status + dimension-narrowing, demand positive evidence, null stays null, clamp at 0) applies at lookup time — cache stays authoritative, prefetch untouched. Honestly documented residual when payload lacks identifier.
- **Failure semantics guard:** `handleTarget` → `executeRequest` inside `withDeadline` → catch routes to `targetFailureResult`: CREATE/CANCEL → `FAIL_CLOSED_BLOCKED` per-item `VALIDATION_UNAVAILABLE` retry-hint; RESCHEDULE → `FAIL_OPEN_NOT_ENFORCED` per-item valid + `ENFORCEMENT_FAIL_OPEN` degradation persisted. `guardedNow` hardens against throwing clock. Structural parse precedes guard (unparseable ⇒ typed `INVALID_QUERY` to thin adapter, not silently valid). Fast-response budget via cached counters + minimal reads.
- **No TOCTOU overclaim:** handler README and editor disclosures state concurrent-checkout residual risk verbatim; caps not promised hard.

## 4. Rollback / recovery & destructive-write safety (Contract §9)

- **Sequence enforced by `src/platform/schedule-mutation/orchestrator.ts`:**
  1. Snapshot full JSON incl. `revision` via `ScheduleGateway.snapshotWorkingHours` → `journal.persistBaseline` **before any write**;
  2. Diff is user-confirmed `MutationPlan` from `computeScheduleDiff` → `diffPreviewModal` explicit consent (Confirm disabled unless zero issues and hash current; focus trap + Escape + restore);
  3. Idempotent writes: deterministic UUIDv5 keys from `(siteId, scopeScheduleId, ruleVersion, weekday/window)` via `src/platform/schedule-mutation/idempotency.ts` (`SCHEDULE_MUTATION_IDEMPOTENCY_NAMESPACE` constant never changes; replay ⇒ `SKIPPED_ALREADY_APPLIED`);
  4. Revision-checked updates: stale revision ⇒ bounded retry (default 3) after re-snapshot fresh `revision`;
  5. Verify: `gateway.verifyApplied` re-read → only then mark `APPLY_COMPLETED`;
  6. Rollback: `gateway.rollbackTo(snapshot)` with fresh rollback keys per snapshot on failure or recovery;
  7. Audit: exactly one `AuditLogEntry` per completed run (`MUTATION_APPLIED` / `MUTATION_FAILED_ROLLED_BACK` / `RECOVERY_COMPLETED`).
- **Crash semantics (T-RB1):** unexpected exception leaves journal `APPLY_IN_PROGRESS`; next run either resumes via `applyNextChange` (idempotent) or `recoverInterruptedApply` restores exact pre-apply window content (`windowContentDiffs` window-granularity, event-id-agnostic) and marks `RECOVERED` with mismatches surfaced verbatim — never prettified.
- **Terminal hardening:** `NON_TERMINAL_STATES={SNAPSHOT_PERSISTED, APPLY_IN_PROGRESS}`; any future terminal state rejected fail-fast with `INVALID_STATE` before gateway/audit — proven by `tests/platform/orchestrator-terminal-states.spec.ts`.
- **Serverless composition:** `beginApply`/`applyNextChange`/`completeApply` are public, durable via `MutationJournalStore`; long applies span invocations.
- **Banned operations gated:** `Update Location`/`Set Service Locations`/`Assign Working Hours Schedule`/`Cancel Event` never auto-invoked; location mutation out-of-scope entirely (`platform` never mutates locations — verified by absence of `SCOPE.DC-MULTILOCATION.MANAGE-LOCATIONS` request).
- **UI mutation lifecycle:** `src/ui/pages/rulesEditorPage.js` polls `bridge.getMutationStatus(planId)` via bounded `pollMutationUntilTerminal` until TERMINAL state, hard-bounded; shows `role="status"` updates; offers **explicit** "Recover interrupted apply" button calling `bridge.recover(scope)` only on click — never auto-retries/reapplies. Double-click guard (`recoverInFlight`) + server idempotency.
- **Rollback honesty:** notes state Cancel Event is terminal (re-create) and past-dated recurrence cannot be recreated (display-only reconstruction) — `src/platform/schedule-mutation/README.md`.

## 5. Billing & entitlements (Contract §7, C2/C5, Blueprint §4 flow 5)

- **Plan recognition (`src/billing/pure/entitlement.ts`):** null/isFree true/missing vendorProductId ⇒ FREE (reliable). Known `vendorProductId` via `VendorProductOverrides` ⇒ that tier. Unknown paid identifier ⇒ `TIER_1` (smallest allowance, fail-safe under-serve) + persistent `UNKNOWN_PLAN_IDENTIFIER` warning + `restrictionReliable:false`. `billingExpirationDate`/`expiresOn` never read (advisory-only, C2). Clone markers ignored.
- **Billable-location count (`src/billing/counter/countBillableLocations.ts`):** pure core takes fetched pages as input (no I/O). Liveness = `archived=false` only (never `status`; `INACTIVE` unsupported). Counted services = non-hidden (`hidden!==true`) regardless of `onlineBooking.enabled` (policy v1). Distinct-set intersection on `locations[type=BUSINESS].business.id` live set; result sorted deterministically; floor `0→1` bumps `count` only, `billableLocationIds` stays true set (reporting vs grant separation — dashboard discloses). Pagination correctness (>50 locations, >100 services) via thin paging adapters (tests/billing/counterAdapters).
- **Coverage selection (`src/billing/pure/coverage.ts`):** `selectManagedLocations` sorts stable: default location first, then byte-wise alphabetical by `locationId`; dedupes; beyond `maxLocations` → `unmanagedLocationIds` (management disabled, configuration preserved — never deleted, upgrade restores). Proved by `tests/billing/downgradeThroughGate.spec.ts`.
- **Enforcement gate (`src/billing/enforcement/entitlementGate.ts`):** implements canonical `EntitlementGate` (`allowedLocationIds` + `meter`). Ports injected; throwing `getAppInstanceSnapshot`/`listManagedLocations`/`countBillable` ⇒ fail-open degraded decision (`degraded:true`, warning persisted in `EntitlementWarningLedger`; dashboard shows persistent alert). Per-source transient warning clearing (fixed `BILL-C3-1`: billing vs listing vs count warnings clear independently). `FAIL_OPEN_RESOLUTION` tier is explicit `null` (no fake plan claim). `overLimit` is not an error — produces stable coverage + upgrade CTA state.
- **Meter endpoint composition (`src/platform/http/meterEndpoint.ts`):** `getEntitlementMeter` verifies caller first (fail-closed 401) → composes `gate.meter()` + `gate.allowedLocationIds()` with per-half failure isolation → always 200 after verification; `meter` and `coverage` halves never corrupt each other. Projector `BillingProjection` fold and `snapshotSource` handle trial-conversion reconciliation (no event exists) — idempotent fold, compaction preserved.
- **Over-limit enforcement & upgrade:** dashboard `src/ui/upgrade/upgradeUrl.js` builds `https://www.wix.com/apps/upgrade/<APP_ID>?appInstanceId=<INSTANCE_ID>` in new tab; identifiers host-injected, never fabricated (unbuildable link degrades to notice text). Pricing tiers in `src/billing/pure/tiers.ts` match constitution (1 / 2-3 / 4-10 / 11+ USD tiers); features identical across tiers.

## 6. Dashboard / accessibility-sensitive behavior

- **Bridge discipline:** `src/ui/services/bridge.js` is ONLY file touching Wix runtime (guarded lazy `import('@wix/essentials').httpClient.fetchWithAuth`); all other UI pages import typed contracts only. `BridgeError` taxonomy (`BRIDGE_NOT_CONFIGURED|TRANSPORT_FAILURE|HTTP_<status>|BAD_RESPONSE`) prevents silent degradation: 404 on meter → `null` (documented n/a), 2xx without pinned DTO → `BAD_RESPONSE`. Dead-port offline run still interactive.
- **Entitlement restriction UX (DASH-C5-1):** `renderRulesEditorPage` loads `getEntitlementMeter()` in background (non-blocking first paint). Healthy coverage: locations outside `allowedLocationIds` are visibly badged ("Not covered by your plan"), inputs disabled, existing config rendered read-only (never deleted); anti-trap rule: any control whose path contributes a validation issue (`issuePaths` including bucket-level overlaps) stays correctable so restriction cannot trap editor in invalid draft. Degraded coverage (`degraded:true`) ⇒ fail-open: persistent `role="alert"` warning banner, no restriction (mirrors enforcement). Meter degraded ⇒ banner without bricking editing. `overLimit` ⇒ upgrade CTA + ordering note ("default location first, then alphabetical"). Missing meter (404 null) or transport failure ⇒ non-blocking notice, unrestricted editor.
- **Validation parity:** provisional `ruleDraftValidators.js` remains frozen; `mirror.js` `setValidationSource` rejects non-conforming server results (fail-closed; previous source stays active), snapshots server issues verbatim — no rewriting of codes/messages/paths.
- **Diff-and-confirm (Contract §9.2):** `diffPreviewModal.js` lists window adds/removes, exception before→after (kind+hours+note), limit before→after with `describeOp`; Confirm disabled unless `canConfirm` (store `canApply()` = modal open + hash current + zero issues) and ops non-empty; double-guarded click handler; `role="dialog" aria-modal="true" aria-labelledby/aria-describedby`, focus moves into dialog on open and restores to previously focused element on close (with `_adoptFocus(null)` fallback documented N-3 coupling — acceptable pre-React port). Escape closes via cancel path.
- **Accessibility:** all controls are native `button`/`input`/`select`; disabled buttons swallow clicks and honours Enter/Space synthesis in `UiNode` kit; every input carries `aria-label` with scope/weekday/index; issues list and degraded banners use `role="alert"`; loading/status regions use `role="status" aria-live="polite"`; `explainPanel.js` renders `RuleOutcome` verbatim (`Blocked/Allowed` + `customerMessage` + `ruleId:code`); meter/coverage pages use proper heading hierarchy, list order preserved verbatim from backend. Dashboard runner `tests/ui/**` includes `accessibility.test.js` (labels/roles/keyboard) green.
- **Honest copy (Contract §12):** locations section discloses "Wix Bookings has no native per-location hours object…through working-hours events"; caps disclosure states TOCTOU residual; counting disclosure on locations usage page states exact billable definition; floor note present; no "100% duplicate-proof" / "hard cap" / unconditional reschedule claims — reschedule labeled best-effort where mentioned.

## 7. Real Wix scaffold assumptions

- **`wix.config.json` linkage proven real:** file exists with `appId: 3e9ec3af-001b-4684-a197-a5133677844d`, `projectId: advanced-booking-rules`, `projectType: App`. Classifier `src/platform/registration/projectConfig.ts` reports `LINKED` (non-empty, non-placeholder string `appId`; placeholders like `GENERATED-BY/REPLACE/PLACEHOLDER` and shape forms `<…>/{{…}}/${…}` rejected; unknown fields tolerated per UQ4/C4). This appId matches `reports/wix-live/BOOTSTRAP_BINDING.md`: authenticated GitHub Actions binding to existing Wix app "Advanced Booking Rules" with that exact ID, real `wix build` succeeded before persistence, no credential persisted. Previous `FIX_BEFORE_INTEGRATION` live finding (CYCLE_32920420147 — "no real wix.config.json") is now repaired; the binding is no longer PLANNED_UNTIL_T_VP0 placeholder but linked.
- **No fabrication:** anti-fabrication sweeps for UUID/hex shapes pass; only RFC2606 `.invalid`/`.example` hosts appear; `DEFAULT_VALIDATION_DEPLOYMENT_URI='/api/bookings-validation'` is internal route, not identifier. `DEFAULT_VENDOR_PRODUCT_OVERRIDES={}` (empty until operator configures real `vendorProductId`s).
- **Scaffold surface honesty:** `src/platform/registration/extensionsManifest.ts` inventory of 8 rows previously `PLANNED_UNTIL_T_VP0` with correct channels (`DASHBOARD_PAGE|DASHBOARD_MODAL|EVENT → UNIFIED_CLI_GENERATE`; `SERVICE_PLUGIN_BOOKINGS_VALIDATION|WEBHOOK_SUBSCRIPTION → APP_DASHBOARD_FALLBACK`; `DATA_COLLECTIONS → INTERACTIVE_CLI_MENU`; `HTTP_ENDPOINTS → FILE_BASED_NO_REGISTRATION`). The linkage upgrade to `LINKED` does not invent extension IDs — IDs remain unregistered until dashboard/config steps complete (per Contract §15 gates `T-VP0`); README §4 makes no registration claim beyond classification. This is precisely the narrow, identifier-free posture required — not a fake registration.
- **Scopes & versioning:** permission set frozen per Contract §5 (no `SCOPE.DC-MULTILOCATION.MANAGE-LOCATIONS`, no write scope for reads); adding permissions later correctly forces major version. `extensions.ts` include updated, `tsconfig.json` and purity roots strengthened — not weakened.
- **Remaining live gates:** empirical `T-VP*`/`T-WH*`/`T-BK*`/`T-RB*` suites still await human-owned dev-site install consent and `WIX_API_KEY` live probes (per Contract §16) — honestly left `OPEN` in `docs/PRODUCT_GATES.json`; product does not claim them. This auditor performed static + deterministic checks only, as mandated.

## 8. Failure posture summary (cross-lane)

| Path | Failure | Behavior | Surfacing |
|---|---|---|---|
| CREATE / CREATE_MULTI_SERVICE | internal error / deadline / counter unavail | fail-closed block with retry hint (`FAIL_CLOSED_BLOCKED`) | per-item block + `ENFORCEMENT_FAIL_CLOSED` sink |
| CANCEL / CANCEL_MULTI_SERVICE | same | fail-closed (classification still enforced) | per-item block; no capacity checks |
| RESCHEDULE (+multi) | internal error / deadline | **fail-open forever** (`FAIL_OPEN_NOT_ENFORCED`), never claims enforcement | per-item valid + `ENFORCEMENT_FAIL_OPEN` sink |
| Entitlement / meter / listing | API transport failure | fail-open, `degraded:true` + warning | dashboard persistent `role="alert"` banner; ledger persists |
| Schedule apply | verify failure / any change failure | rollback to snapshot + `MUTATION_FAILED_ROLLED_BACK` audit | status region + recovery UI |
| Webhook ingest | duplicate / out-of-order / crash mid-dispatch | dedup by envelope `id`; order via `entityEventSequence`; at-least-once dispatch → exactly-once effect via deliveryKey idempotency; buffered gap drain + authoritative reconciliation | chaos suite proven |

All failure branches emit explicit incidents via `DegradationSink`/`EntitlementWarningLedger`/audit collection — no silent degradation.

## 9. Verdict rationale

The SHA under audit is mechanically the accepted base plus the authenticated scaffold binding (`wix.config.json` linked to the real app 3e9ec3af-… per bootstrap evidence) and the additive registration classifier/surface that previously earned an integrated ACCEPT. Re-derivation confirms frozen contracts intact, seven-root purity green, DTO parity verbatim, six→three target mapping welded to registration, enforcement covering every bulk index with correct fail-closed/open semantics, CANCEL/RESCHEDULE matrix bit-correct, subject-facts seam default-inert and evidence-gated, counters prefetched with isolated degradation, webhook dedup/ordering/crash recovery proven, schedule mutation obeying all nine §9 gates with bounded revision retry and terminal-state hardening, billing fail-open with per-source warning clearing and no data deletion on downgrade, dashboard restriction mirroring enforcement coverage with fail-open degraded posture and anti-trap unlock, accessibility roles/focus/labels present, disclosures honest per §12, and `wix.config.json` linkage now real — not fabricated. No blocking cross-system contract violation, no hidden unsupported Wix assumption, no scope creep, no PREVIEW_GATED dependency.

Non-blocking observations (cosmetic, no repair required): placeholder-token false-positive in safe direction; `..` vs percent-encoded traversal in deploymentUri validator (self-authored value); manifest kind vocabulary split (`SERVICE_PLUGIN_BOOKINGS_VALIDATION` vs `SERVICE_PLUGIN`) — all documented and inert.

VERDICT: ACCEPT
