{
  "lane": "rules",
  "task": "RULES-C8-DG: Diagnose and repair the deterministic gate failure (typecheck + purity + vitest) against the accepted base at ec916b75d5600e02d679d264648ac92333d721f1. Investigate all domain source and test files under src/domain/ and tests/domain/ for type errors, import resolution failures, test regressions, and purity violations. Run npm run check against the accepted base to reproduce the exact failure, trace it to root cause, and produce a minimal repair that restores green deterministic gates without altering governance, shared DTOs, or platform/adapters/billing/dashboard code.",
  "reason": "Integration lane has stagnated:13+ consecutive factory runs all exited 'final_auditor_unavailable_or_failed' on the same integration task. The deterministic gate (npm run check) now fails, which is a pure code-level regression requiring investigation in the domain layer. Rules is the only lane that owns pure TypeScript (src/domain/**, tests/domain/**) with zero Wix/credential dependencies, making it the correct lane to diagnose and repair deterministic gate failures. Per AGENTS.md loop-health policy, stagnation demands a materially different repair strategy — selecting rules breaks the integration retry loop and addresses the actual failing gate. Rules/dashboard/billing no-op completions in cycle 7 did not prove any PRODUCT_GATES.json entry; the rules_domain gate remains OPEN and is the foundational dependency for all other lanes."
}
