{
  "lane": "integration",
  "task": "INT-REPAIR-DET-GATE",
  "reason": "The integrated deterministic gate (npm run check: tsc + purity + vitest) failed at base SHA ec916b75d5600e02d679d264648ac92333d721f1. Integration is the only active lane with outstanding product-code changes (registration surface, scaffold classifiers, composition roots, validation-plugin wiring). Rules, Dashboard, and Billing are all complete with zero product diff in the last integrated cycle — their code cannot be the source of a new deterministic regression. The 14 consecutive factory runs that dispatched integration all exited 75 (builder/OX failure), meaning the deterministic gate was never re-verified against a fresh candidate. The next integration candidate must (1) reproduce the failure locally (npm run check), (2) identify whether it is a tsc type error, purity-gate violation, or vitest regression in platform-owned code, (3) produce a minimal repair that restores tsc --noEmit, purity gate over all protected roots, and full vitest suite green, and (4) ensure the repaired candidate passes audit before integration. This task advances the product by unblocking the deterministic gate that every subsequent cycle depends on."
}
