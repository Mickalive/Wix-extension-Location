{
  "lane": "integration",
  "task": "INT-C8-STABILIZE: Diagnose and repair the deterministic gate failure (exit 75 across 14 consecutive cycles). Run npm run check, npm run build, and the full test suite on the current HEAD (ec916b75). Identify the specific compilation error, test failure, or purity-gate regression causing the deterministic gate to fail. Apply the minimal code fix that restores exit 0 across all deterministic checks (tsc --noEmit strict, purity gate over all protected roots, vitest suite). If the failure is caused by the removal of control-plane workflows or agent file drift, repair only the product-adjacent scaffolding (extensions.ts, wix.config.example.json, tsconfig.json, .gitignore) — never reintroduce deleted workflows. This task must produce a candidate where npm run check && npm run build pass before any further Wix-binding work (INT-C7-LIVE) is attempted.",
  "reason": "Stagnation requiring materially different repair strategy: 14 consecutive factory runs (CYCLE_33069918456 through CYCLE_33072886087) produced identical integration builder failure (exit code 75). The user-reported context confirms 'Fresh integrated deterministic gate failed.' Rules, Dashboard, and Billing lanes are all complete (verified by integrated audit ACCEPT in run 32920420147). The previous approach — advancing INT-C7-LIVE (authenticated Wix binding) directly — has repeatedly failed before reaching Wix authentication because the deterministic code-quality gate does not pass. Per loop-health policy, the hypothesis must change materially: instead of attempting the full Wix-binding task, first stabilize the deterministic gate as a prerequisite. Source evidence: reports/factory/CYCLE_33069918456.md through reports/factory/CYCLE_33072886087.md (all exit 75), docs/NEXT_CYCLE.json (cycle 7 queue), docs/state.json (last_result NOT_READY, last_reason final_auditor_unavailable_or_failed), reports/audits/CYCLE_32920420147_INTEGRATED.md (last successful integration with 548/548 passing)."
}
