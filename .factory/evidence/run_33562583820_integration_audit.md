# Factory Lane Audit — Integration Candidate 8a38eec35dd42a4de9956684f63754c58dccf40e

## Candidate and base
- **Candidate SHA:** `8a38eec35dd42a4de9956684f63754c58dccf40e` (tag `factory-candidate/integration/238`, message `candidate(integration): generation 238`)
- **Lane repair base:** `704a639c1d03a1556d185efdbc0f009ea9b32063` (`candidate(integration): generation 234`, author `wix-official-scaffold`)
- **Audit scope:** exact integration candidate only; inherited cumulative changes before the repair base are excluded from this builder's accountability per workflow instruction.
- **Checkout:** detached HEAD at candidate SHA; workspace `/home/runner/work/_temp/wix-factory-33562583820/product`

## Diff against repair base (reproduced)
```
git diff 704a639c1d03a1556d185efdbc0f009ea9b32063..8a38eec35dd42a4de9956684f63754c58dccf40e
```
Result: exactly 2 files, 4 deletions:
- `src/env.d.ts`: removed `/// <reference path="../.astro/types.d.ts" />` and auto-generated note
- `tsconfig.json`: removed `".astro/types.d.ts"` from `include`

No other files changed between repair base and candidate. No domain, billing, dashboard, platform logic, tests, or `wix.config.json` edits.

## Wix-owned scaffold / binding provenance (authenticated official generation)
Inspected via allowed `git show` on `origin/main`:

- `git show origin/main:.factory/evidence/run_33321707099_official_scaffold.json`:
```json
{
  "schemaVersion": 3,
  "source": "authenticated official Wix existing-app scaffold",
  "appId": "3e9ec3af-001b-4684-a197-a5133677844d",
  "projectId": "advanced-booking-rules",
  "projectType": "App",
  "createNewVersion": "0.0.105",
  "wixCliVersion": "1.1.238",
  "generatorExit": 1,
  "projectAcceptedDespiteOptionalPostTaskFailure": true,
  "pristineWixBuild": "PASS",
  "scaffoldPackageSha256": "1768e7a61f8e81751712fe4d3abd985aeee9df012cc98b35c2d5dcaad97c98cd",
  "developmentSiteProvisioned": true,
  "secretsPersisted": false
}
```
- `git show origin/main:.factory/evidence/run_33321707099_official_scaffold_pristine_build.txt`: pristine Wix build completed successfully (`✓ Completed in 320ms`, `✓ built in 2.49s`, `✓ built in 7.81s`, `Server built in 10.70s`, `Complete!`, 6864 modules transformed), confirming scaffold was generated by official `wix` toolchain, not hand-authored.

Binding check:
- `git show 8a38eec:wix.config.json` → `{"appId":"3e9ec3af-001b-4684-a197-a5133677844d","projectId":"advanced-booking-rules","projectType":"App"}`
- `git show 704a639:wix.config.json` → identical
- Matches official scaffold `appId`/`projectId` verbatim. App ID preserved, no fabrication, no rotation.

Repair base author is `wix-official-scaffold`, confirming the scaffold files (`astro.config.mjs`, `package.json`, `package-lock.json`, `wix.config.json`, `tsconfig.json`, `src/env.d.ts`) originate from authenticated generation. Candidate does not hand-author guessed scaffold values; it only trims ephemeral generated types.

Authenticated Wix build and runtime evidence intentionally not reproduced here per instructions (belongs to WIX_QA/final gate); missing credentials not treated as FIX.

## Lane ownership and scope compliance
- Integration lane owns `Wix CLI scaffold/project metadata`, `platform adapters`, `extension/backend transport` per `AGENTS.md` and `docs/BUILD_BLUEPRINT.md`.
- Modified paths `tsconfig.json` and `src/env.d.ts` are explicitly scaffold metadata owned by integration. No modification to `src/domain/**`, `src/billing/**`, `src/ui/**`, `src/platform/adapters/**`, `src/platform/composition/**`, `tests/**` except scaffold hygiene.
- No lane widening, no cross-lane file edits, no `wix.config.json` App ID mutation.
- Inherited diff `origin/main..candidate` contains extensive `.factory/evidence`, `src/platform`, `src/domain`, `src/billing`, `tests`, etc., but `git diff --name-only origin/main..704a639` shows those are part of the repair base generation 234, not introduced by this repair generation 238. Not misclassified.

## Deterministic credential-free evidence reproduced
All checks run locally at candidate SHA without pipes/redirects, without `wix build`:

1. `npm ci` — succeeded (960 packages, 33s), warnings only peer dependency overrides, no failure.
2. `npm run typecheck` (`tsc --noEmit`) — **PASS** after `npm ci` (previously failing before node_modules install; after install clean pass; candidate's removal of `.astro/types.d.ts` prevents missing-file error in CI where `.astro` is not generated).
3. `npm run check:purity` (`node src/platform/purity/check-purity.mjs`) — **PASS**: `Purity gate passed: no '@wix/' imports under src/domain, src/billing/pure, ...`
4. `npm test` (`npm run test:unit` = `check:purity && vitest run --config src/platform/vitest.config.ts`) — **PASS**: 49 test files, 548 tests passed. Full output includes platform, domain, billing suites:
   - domain: evaluate, exceptions, duplicates, windows, etc.
   - platform: validation-plugin bulk/handler/counters/entitlement, webhooks chaos/pipeline, http-auth/mutations, schedule-mutation, idempotency, registration-surface, composition-root, etc.
   - billing: counter, entitlement, projection, coverage, tiers, etc.
   - Purity gate fixture tests passed.

No test weakening, no skipped checks, no hidden degraded states observed in diff. Tests are adversarial regressions, not implementation mirrors (existing suites cover DST, split windows, pagination, idempotency, fail-open/closed semantics).

## Contract and blueprint alignment
- Respects `docs/WIX_TECHNICAL_CONTRACT.md` §5 scope hygiene, §9 destructive-write protections (no schedule mutation in this diff), §10 classifications (no PREVIEW_GATED claim).
- Respects `docs/BUILD_BLUEPRINT.md` ownership boundaries; no pure-domain Wix imports introduced.
- No secrets committed, no `~/.wix` access, no Wix API mutation, no publish/release.

## Findings
No reproducible findings requiring repair. The two deletions are a valid deterministic-CI hygiene fix:
- Rationale: `.astro/types.d.ts` is a generated ephemeral artifact not present in clean CI; referencing it makes `typecheck` brittle. Removing it from `tsconfig.include` and `env.d.ts` reference allows credential-free `tsc --noEmit` to pass without requiring `wix build` to generate `.astro`. This matches prior integration auditor guidance for scaffold determinism and does not alter runtime behavior.
- Verification: typecheck and 548 tests pass; pristine official scaffold build evidence remains authoritative for runtime; candidate preserves binding.

No semantic regression, no fake evidence, no weakened tests, no unsupported Wix assumptions.

## Verdict
Candidate satisfies assigned repair, preserves authenticated official scaffold binding, modifies only lane-owned scaffold metadata, and passes all credential-free deterministic gates.

VERDICT: ACCEPT
