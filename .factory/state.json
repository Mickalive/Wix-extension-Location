{
  "architecture": "lane-machine/1",
  "generation": 34,
  "phase": "PLAN",
  "lane": "integration",
  "accepted_base": "ec916b75d5600e02d679d264648ac92333d721f1",
  "candidate": null,
  "repair_feedback": "xceptions.ts`, `duplicates/duplicates.ts`, `time/wallClock.ts`, `time/intlZone.ts`.\n- Timezone/DST handling is present (`wallClock.ts`, `intlZone.ts`), satisfying the timezone/DST quality bar.\n\n### Integration (platform adapters, enforcement, mutation, webhooks)\n- **Validation-plugin handlers** (`src/platform/validation-plugin/handlers.ts`) implement the binding failure semantics (Contract §5.3): CREATE/CANCEL (+ `_MULTI_SERVICE`) fail-closed with per-item block-with-retry-hint; RESCHEDULE (+ `_MULTI_SERVICE`) fail-open forever with `enforcementClaim: 'FAIL_OPEN_NOT_ENFORCED'` and a logged degradation. Every item index gets an explicit result (no silent gaps). Verified the target-semantics guard (`targetFailureResult`), the guarded clock fallback, and the entitlement/counter/duplicate degradation paths.\n- **Targets** (`targets.ts`) derive semantics from the single source `failureSemanticsFor` in `src/shared/errors.ts`; `evaluationTargetOf` collapses the six targets onto the three-operation union. No drift between registered and enforced surface.\n- **Schedule mutation** (`src/platform/schedule-mutation/orchestrator.ts`) implements the full snapshot → diff → apply → verify → rollback sequence (Contract §9): baseline persisted before any write, deterministic UUIDv5 idempotency keys, revision-checked updates with bounded retries, verify-before-mark-applied, rollback on failure, single audit entry per run, and crash-mid-apply recovery (`recoverInterruptedApply`) that restores the exact pre-apply snapshot. Terminal-state hardening rejects re-apply/re-rollback after terminal states. Idempotency (`idempotency.ts`) uses an application-defined UUIDv5 namespace (not a Wix identifier) and fresh rollback keys per snapshot.\n- **Webhooks** (`src/platform/webhooks/pipeline.ts`, `envelope.ts`) implement dedup on envelope `id`, per-entity ordering via `entityEventSequence`, durable buffering for gaps, superseded-skip, and crash/exactly-once-effective semantics via claim → dispatch → advance → complete with per-key handler idempotency. Signature rejection happens before any store interaction (fail-closed, zero mutation). Structural validation of the envelope is mandatory (no duck-typing bypass).\n- **Registration surface** (`src/platform/registration/**`): `extensions.ts` is intentionally empty (anti-fabrication, pending T-VP0); `extensionsManifest.ts` declares all entries `PLANNED_UNTIL_T_VP0`; `validationExtension.ts` derives targets from the implemented matrix (single source of truth); `projectConfig.ts` classifies `wix.config.json` with a safe under-reporting bias (LINKED only on positive non-placeholder appId evidence). No fabricated IDs or credentials.\n\n### Dashboard (UI)\n- `src/ui/services/bridge.js` is the only module referencing Wix runtime, via a guarded lazy dynamic import; offline it fails safely with typed `BRIDGE_NOT_CONFIGURED`. It strictly validates the pinned entitlement-meter DTO (`isEntitlementMeterDto`) and the mutation-lifecycle envelopes — a drifted/empty 2xx body is `BAD_RESPONSE`, never silently interpreted as data.\n- `src/ui/pages/locationsUsagePage.js` renders the pinned DTO with honest counting copy, persistent degraded-warning banner (never renders silently healthy), stable-ordering disclosure, no-deletion reassurance, and an upgrade CTA that only renders when identifiers are validly injected (never fabricated). Accessibility: `role=\"status\"` for loading, `role=\"alert\"` for degraded/error, native buttons/anchors.\n- `src/ui/validation/mirror.js` and `src/ui/dom/kit.js` confirm the dashboard lane is testable offline and does not fork domain semantics.\n\n### Billing\n- **Entitlement gate** (`src/billing/enforcement/entitlementGate.ts`) implements the ratified fail-open posture: a billing/listing/counting failure never blocks a paying merchant's booking; degraded decisions carry `degraded: true` plus a persisted warning; over-limit is a normal decision (not an error) with stable coverage ordering and no deletion of customer configuration. Per-source warning liveness (each transient code clears when its own source heals).\n- **Counter** (`src/billing/counter/countBillableLocations.ts`, `countFromAdapters.ts`) implements the ratified billable-location definition (non-archived location referenced by a non-hidden service via `locations[type='BUSINESS'].business.id`), with the single-location floor (computed 0 → 1) and distinct-set intersection to prevent double counting. Paging driver drains adapters with a runaway guard.\n- **Coverage** (`src/billing/pure/coverage.ts`) implements stable ordering (default location first, then alphabetical by id) and returns unmanaged ids without deletion — downgrade safety preserved.\n- **Projection** (`src/billing/projection/fold.ts`) is order- and duplicate-safe by construction (sorted by sequence+id, idempotent transitions); `PAID_PLAN_AUTO_RENEWAL_CANCELLED` keeps paid identifiers (cancelled-until-expiry never downgrades); reconciliation supremacy via `seedPlanViewFromSnapshot`.\n- **Composition** (`src/platform/composition/entitlementComposition.ts`) wires projector → snapshot source → gate (+ meter) → reconciliation seam, consuming accepted billing exports unforked and importing zero webhook types into the enforcement path.\n\n### Cross-lane DTO/type parity\n- The pinned entitlement-meter DTO `{meter:{count,degraded}, coverage:{allowedLocationIds,overLimit,degraded,warning}}` is produced by `meterEndpoint.ts` (with per-half failure isolation, always 200 after auth) and consumed verbatim by the dashboard bridge with strict shape validation. No forked semantics, no bypassed bridge, no weakened validation.\n\n## 3. Failure / rollback behavior\n\n- **Booking-time enforcement:** CREATE/CANCEL fail-closed (a booking that cannot be validated must not happen); RESCHEDULE fail-open forever (best-effort, never claims enforcement). Entitlement/counter/duplicate failures degrade visibly with persisted incidents — never silent, never thrown into the booking decision.\n- **Schedule mutation:** snapshot-before-write, idempotent replay, verify-before-commit, rollback on failure, crash recovery restoring exact pre-apply state, single audit entry per run. No silent destructive schedule rewrites.\n- **Webhooks:** dedup, ordering, buffering, superseded-skip, and crash/exactly-once-effective recovery.\n- **Billing:** fail-open on all infrastructure errors; over-limit never deletes configuration; downgrade never deletes customer data.\n\n## 4. Anti-fabrication, honesty, scope\n\n- No fabricated Wix identifiers, credentials, or capabilities. `extensions.ts` empty by design; registration entries `PLANNED_UNTIL_T_VP0`; no production-capability claims beyond what the contract classifies.\n- `docs/PRODUCT_GATES.json` honestly keeps all gates `OPEN` (including `real_wix_scaffold_registration`, `empirical_wix_validation`, `real_wix_build_release`) — no gate is marked PROVEN without evidence.\n- The candidate's `.astro/` change is in-scope (scaffold/build-output hygiene) and harmless.\n\n## 5. CRITICAL FINDING — committed `wix.config.json` contradicts the `.gitignore` and README contract\n\nThe central issue this audit must resolve:\n\n- **`.gitignore` (line 20)** explicitly and emphatically states: `wix.config.json` — \"Real Wix CLI project binding … Holds account-bound identifiers; **never commit** or hand-fabricate.\"\n- **`src/platform/registration/README.md` §1** states the file is \"**gitignored** … **never committed, never hand-written**.\"\n- **Yet `wix.config.json` IS committed in the tree at HEAD**, containing a real-looking appId `3e9ec3af-001b-4684-a197-a5133677844d`, `projectId: advanced-booking-rules`, `projectType: App`.\n- The candidate's only change (adding `.astro/`) did **nothing** to reconcile this contradiction.\n\n### Assessment of the committed file's legitimacy\n- `reports/wix-live/BOOTSTRAP_BINDING.md` documents that the trusted workflow (GitHub Actions, protected Wix API key) authenticated and bound to the existing Wix app \"Advanced Booking Rules\" (App ID `3e9ec3af-001b-4684-a197-a5133677844d`), generating a real `wix.config.json` and completing a real `wix build` before persisting. No credentials were persisted.\n- `docs/NEXT_CYCLE.md` confirms the intent to \"persist only the generated non-secret `wix.config.json` metadata.\"\n- `AGENTS.md` (top governance) authorizes the integration lane to \"repair the real non-secret `wix.config.json` only while preserving the bound existing App ID.\"\n\n**Conclusion on legitimacy:** the committed `wix.config.json` is the legitimate, real binding — authenticated by the trusted workflow, non-fabricated, non-secret (only appId/projectId/projectType), and governance-authorized. It does **not** violate the anti-fabrication contract.\n\n### Conclusion on the gitignore/README contract\nThe committed state **does not comply** with the repo's own `.gitignore` and README contract, which still assert the file should never be committed. The candidate did not reconcile this. This is a genuine, unresolved contract inconsistency:\n\n1. The `.gitignore` rule is now dead/misleading for this file (once a file is tracked, gitignore has no effect on it), but it still actively instructs maintainers to never commit it.\n2. The README makes a false claim (\"never committed, never hand-written\") that contradicts the committed state.\n3. Operational risk: a future agent or maintainer reading the `.gitignore`/README could \"fix\" the situation by removing the file from tracking — silently losing the real binding — or be misled about whether the committed file is legitimate.\n4. The prior integrated audit (CYCLE_32920420147) explicitly verified \"**No `wix.config.json` committed**\" as a point of ACCEPT; this cycle's state has materially changed that fact without updating the governing documentation.\n\nThe correct resolution is to reconcile the `.gitignore` and README with the now-committed real binding (e.g., update the `.gitignore` to allow the real `wix.config.json` while keeping the placeholder template committed, and update the README to reflect that the authenticated binding is now persisted). The candidate did not perform this reconciliation.\n\n## 6. Non-blocking observations (recorded; not the basis of the verdict)\n\n1. **O1:** `docs/PRODUCT_GATES.json` and `docs/state.json` remain `OPEN`/`NOT_READY` — consistent with the release audit not having completed; not a defect.\n2. **O2:** `BOOTSTRAP_BINDING.md` claims a real `wix build` completed, but the `real_wix_build_release` gate is still `OPEN`. This is consistent with the gate not yet being formally marked PROVEN by the release audit; the binding evidence exists but the gate ledger has not advanced.\n3. **O3:** The `.gitignore` comment and README were written for the pre-binding state and are now stale (this is the substance of the critical finding, not a separate observation).\n\n## 7. Verdict rationale\n\nThe product code across all four lanes is well-engineered and internally consistent: pure deterministic domain, binding failure semantics, snapshot→diff→apply→verify→rollback with crash recovery, fail-open billing posture, strict DTO parity, honest anti-fabrication, and honest gate accounting. The candidate's own change (`.astro/` in `.gitignore`) is correct and harmless.\n\nHowever, the integrated state at this SHA contains a genuine, unresolved contract inconsistency: `wix.config.json` is committed with a real appId while the repo's own `.gitignore` and README explicitly and emphatically state it should never be committed, and the candidate did nothing to reconcile this. The committed file is legitimate and governance-authorized, but the governing documentation was not updated to reflect the new reality, creating a real operational risk (a future maintainer could remove the binding from tracking) and a false contract claim. This is exactly the kind of cross-system inconsistency this integrated audit exists to catch, and it must be repaired before the state is accepted.",
  "lease": null,
  "last_run": 33324923449,
  "last_transition": {
    "reason": "integrated_audit_fix",
    "run_id": 33324923449
  },
  "last_operational_failure": null
}
