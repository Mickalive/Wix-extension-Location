name: Wix Product Factory [PAUSED]

'on':
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  paused:
    name: Factory paused for architecture benchmark
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Wix Product Factory is intentionally paused. No product mutation, agent execution, Wix call, persistence, or handoff is performed."
