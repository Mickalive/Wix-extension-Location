name: Wix Factory Watchdog [PAUSED]

'on':
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  paused:
    name: Watchdog paused for architecture benchmark
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Wix Factory Watchdog is intentionally paused. No cancellation, dispatch, branch cleanup, or run cleanup is performed."
