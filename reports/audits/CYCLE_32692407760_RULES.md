# Persisted recovery audit — RULES, cycle 32692407760

Original independent verdict: `FIX_BEFORE_INTEGRATION`.

Mandatory blockers to repair before new feature work:

1. `src/domain/time/wallClock.ts` imports `isValidWindowStartMinute` from the wrong module. It is exported from `src/domain/model/primitives.ts`, not `time/intlZone.ts`. This causes TS2305 and cascading runtime `TypeError` failures. Fix import and run strict typecheck/full suite.
2. `tests/domain/evaluate.test.ts` and `tests/domain/ruleset.validate.test.ts` use wrong relative import depths. Fix to `../../src/domain` and `./helpers/*` as appropriate.
3. Five assertions remain red after load repair. Four are test-fixture/expectation bugs, not implementation bugs: two duplicate tests confuse local wall time with UTC and therefore do not actually overlap; New York DST expectation after 2026-03-08 uses EST instead of EDT; spring-forward 02:30 must advance to the next valid local time 03:00, not 03:30; Lord Howe 02:05 must advance to 02:30. Correct tests without changing correct implementation behavior. Add a genuinely overlapping identity-key conflict test so `IDENTITY_TIME_CONFLICT` is proven.
4. Real implementation defect: a slot ending exactly at local midnight is falsely classified as overnight. Treat an end instant at next-day 00:00 as exclusive minute 1440 for window fitting; keep genuine overnight spans blocked. Add regressions: 23:30–24:00 under `[0,1440)` ALLOW; 23:30–00:30 BLOCK.

Non-blocking hardening to do during repair when safe: remove literal `@wix/*` prose tripwire in domain README; document cross-midnight duplicate v1 limitation; deduplicate reserved rule IDs by importing canonical constants; strengthen negative-control duplicate tests so only the intended discriminator differs.

The independent auditor otherwise found the architecture/model largely sound and verified split windows, exception precedence, cap semantics, site-zone day bucketing, identity-free duplicate behavior, entitlement coverage, failure classification, explanations, determinism, and domain purity once mechanical load errors were repaired.

Source of truth remains immutable branch `cycle/wix-build/32692407760/audit-rules`.

VERDICT: FIX_BEFORE_INTEGRATION
