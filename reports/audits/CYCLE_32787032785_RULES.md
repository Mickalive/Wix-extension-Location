# Audit — CYCLE 32787032785 / RULES lane (RULES-C2-1-REPAIR)

- **Auditor:** lane-auditor (rules), independent
- **Date:** 2026-08-24
- **Candidate:** `/tmp/wix_rules_candidate` @ `8663cf2` ("Wix build 32787032785: rules candidate (active)")
- **Accepted base:** `53f51d9` ("Wix build 32692407760: director attempt") — current checkout, untouched
- **Task:** `docs/NEXT_CYCLE.json` → `lanes.rules` (`RULES-C2-1-REPAIR`, mandatory repair of `RULES-C1-1`)
- **Binding references:** `docs/WIX_TECHNICAL_CONTRACT.md`, `docs/BUILD_BLUEPRINT.md`, `directives/RULES.md`, prior audit `reports/audits/CYCLE_32692407760_RULES.md`

---

## 1. Scope and real-diff inspection

`git diff --name-status 53f51d9..8663cf2`: **23 files, all status `A` (additions)**, exclusively under
`src/domain/**` (12 files incl. README) and `tests/domain/**` (11 files). Verified negatively:

- Zero diff lines against every immutable path (`MAIN_PROMPT.md`, `.github/**`, `.opencode/**`,
  `opencode.json`, `AGENTS.md`, `directives/*`) and against cross-lane paths (`src/platform/**`,
  `src/shared/**`, `tests/platform/**`, `package.json`, `tsconfig.json`).
- `src/domain/ports.ts` is **byte-for-byte identical** to the accepted base (SHA-256
  `af68e698ae57a5f15ff02eae10141de9dfb3230002a530c52e93f3f7eb637fbc` in both trees) — the canonical
  contract was consumed, not forked, exactly as `canonical_contracts_notice` requires.
- Hygiene scans: no `@wix/` import anywhere in `src/domain` (only the pre-existing prose comment inside
  the frozen `ports.ts`, which both purity scanners strip); no I/O, network, `console`, `process.env`,
  host-clock reads, TODO markers, fabricated UUIDs/identifiers, or secret-like strings in any new file.
- **No feature creep.** Every change maps to a repair-brief item (B1–B4 blockers, A1–A5 opportunistic
  fixes). The cycle-1 `.test.ts` → `.spec.ts` rename is a *necessary* repair detail: the accepted
  harness glob is `tests/**/*.spec.ts` (`src/platform/vitest.config.ts`), so the old names would never
  have been picked up by the repo runner.

**Scope verdict: clean.**

## 2. Executable checks actually run (none hand-waved)

Executed inside the candidate worktree (Node v22.13.0, Linux, zero credentials, zero network egress;
candidate `node_modules` matches the committed lockfile):

| Check | Result |
|---|---|
| `npx tsc --noEmit` (strict, `noUncheckedIndexedAccess`, whole project incl. src/domain + tests/domain) | **PASS, 0 errors** |
| `npm run check:purity` (`check-purity.mjs` over `src/domain`, `src/billing/pure`) | **PASS** |
| `npx vitest run --config src/platform/vitest.config.ts` | **126 / 126 passed, 0 skipped, 0 failed** (13 files: 93 domain + 33 pre-existing platform tests stay green) |
| Full `npm run check` (typecheck + purity + vitest) | **PASS**, repeated twice — deterministic, no flake |
| Grep for `.only/.skip/todo` markers | none |

The `PURITY GATE FAILED` text visible mid-suite is the platform suite's own negative-fixture test
(deliberate violation written to `/tmp/purity-gate-fixture-*`), proving the gate catches real
violations — expected output, suite exit 0.

**Independent adversarial probes** (my own hand-derived expectations, built as a scratch Vitest harness
in `/tmp/opencode/rules-probe-c2` importing the candidate barrel directly; removed after the run):

1. **Cycle-1 B4 original repro re-executed:** `2026-08-27T03:30Z..04:00Z` (23:30–24:00 EDT Wed) under
   `loc-1 WED [00:00,24:00]` ⇒ **allow** (was `block/overnight_slot` in cycle 1). Genuine overnight
   `…03:30Z..04:30Z` (23:30–00:30) ⇒ **block, OUTSIDE_BOOKING_HOURS** even under `[0,1440)`.
   Midnight-ending slot under an 18:00-closing window ⇒ block. `resolveSlot` returns
   `{endMinute:1440, crossesMidnight:false}` for exact-midnight ends and
   `{endMinute:30, crossesMidnight:true}` for past-midnight ends.
2. **Fall-back day:** slot Nov 1 23:30 EST → Nov 2 00:00 EST (`2026-11-02T04:30Z..05:00Z`) normalizes
   to endMinute 1440 of Sunday Nov 1 and ALLOWS under `SUN [0,1440)`; the 25-hour day's cap bucket is
   correctly `2026-11-01T04:00Z .. 2026-11-02T05:00Z`. (My first probe fixture here was arithmetically
   wrong — 04:30Z is pre-transition EDT — and the implementation's answer was correct; fixed my probe,
   not the code.)
3. **Lord Howe half-hour zone:** midnight-ending slot resolves `startMinute 1410 / endMinute 1440`
   and fits its day window.
4. **Duplicates ground truth:** `WED 810..870` == `17:30Z–18:30Z`; overlapping existing
   `17:00Z–18:00Z` same service ⇒ `DUPLICATE_BOOKING`; same key + different service + overlap ⇒
   `IDENTITY_TIME_CONFLICT` end-to-end; same key + SAME service ⇒ `DUPLICATE_BOOKING` only (strongest
   signal wins, no double-count); DECLINED/WAITING_LIST/CANCELED never block; malformed existing
   records cannot block.
5. **DST resolver policies re-derived:** NY 2026-03-08 02:30 gap → `07:00Z` (03:00 EDT);
   Lord_Howe 2026-10-04 02:05 → `15:30Z` (02:30 LHDT); fall-back 01:30 → first occurrence `05:30Z`.

All probes passed. Worktree left clean (probes lived outside it).

## 3. Repair-brief compliance (blocker by blocker)

| Finding | Repair verified |
|---|---|
| **B1** bad import specifier in `wallClock.ts` | `isValidWindowStartMinute` imported from `../model/primitives` (line 16–19); TS2305 gone; cascading TypeError gone (full suite loads and passes) |
| **B2** unloadable suites | `tests/domain/evaluate.spec.ts` + `ruleset.validate.spec.ts` use depth-2 `'../../src/domain'` and `'./helpers/*'`; both collect and pass |
| **B3(1)(2)** wall-time/UTC fixture confusion | Existing bookings moved to genuinely overlapping instants (17:00Z–18:00Z vs proposal 17:30Z–18:30Z); proposal↔UTC equivalence asserted in-test (`PROPOSAL_START_Z` guard) and re-derived independently |
| **B3(3)** EST-after-DST-start expectation | Now expects date `2026-03-08`, minute 1350 (22:30 EDT) with §4.7 comment |
| **B3(4)** spring-forward 02:30 | Now expects `07:00Z` (next valid local time 03:00 EDT) with §4.7 comment; consistent with sibling 02:00 case |
| **B3(5)** Lord_Howe 02:05 | Now expects `15:30Z` (02:30 LHDT after the 30-minute shift) with §4.7 comment |
| **B3 consequence** identity-key path unproven | New genuinely-overlapping `IDENTITY_TIME_CONFLICT` test asserts the full outcome shape end-to-end (acceptance criterion 5 of RULES-C1-1 now proven) |
| **B4** false overnight_slot at local midnight | `resolveSlot` normalizes end==next-day 00:00 to exclusive minute 1440 (only when `endDate!==targetDate && minutesOfDay===0`); evaluator blocks only `crossesMidnight`; regressions committed: 23:30–24:00 ALLOWs under `[0,1440)`, 23:30–00:30 BLOCKs as overnight, midnight-end under partial window still blocks |
| **A1** README prose tripwire | Reworded to "zero Wix SDK imports"; additionally the CI scanner only reads code extensions (`.md` never scanned) |
| **A2** cross-midnight duplicate hole | Documented in README and module docstring (start-bucket convention, revisit at T-VP3) |
| **A3** reserved-id drift | `validate.ts` imports `RESERVED_RULE_IDS` from primitives; regression test iterates the constant itself |
| **A4** vacuous negative controls | Day/key-inequality/key-absence controls now overlap the proposal so the asserted dimension is the only discriminator |
| **A5** unverified-before-submit | Delivered state is green as-is (cycle-1's B1/B2 class of errors would have been impossible to miss) |

No implementation behavior was changed to satisfy wrong expectations: cases B3(3)–(5) keep the
contract-correct resolver behavior and fix only the fixtures, exactly as the prior audit directed.

## 4. Acceptance criteria scorecard (RULES-C2-1-REPAIR)

| # | Criterion | Status |
|---|---|---|
| 1 | Strict tsc (`noUncheckedIndexedAccess`) zero errors over src/domain + tests/domain | **PASS** (whole-project `tsc --noEmit` exit 0) |
| 2 | Full Vitest suite green, zero skipped, incl. new genuinely-overlapping IDENTITY_TIME_CONFLICT test | **PASS** (126/126, 0 skipped, twice) |
| 3 | 23:30–24:00 under `[0,1440)` ALLOWs; 23:30–00:30 BLOCKs overnight_slot; regressions committed | **PASS** (+ independent repro of the audit's original failing case) |
| 4 | Zero `@wix/` imports under src/domain; `ports.ts` unchanged byte-for-byte | **PASS** (gate exit 0; SHA-256 identical) |
| 5 | All five B3 fixture corrections present with §4.7 citations | **PASS** (each carries a comment citing Contract §4.7 semantics) |
| 6 | Fresh independent lane audit ends VERDICT: ACCEPT | **This audit** |

## 5. Non-blocking observations (for the Director / future cycles — no repair required)

- **N1:** Identity-free duplicate matching still buckets by the existing booking's START day (documented
  v1 limitation A2). Fine for now; must be revisited with real payload evidence at gate T-VP3 before any
  duplicate-protection marketing claim.
- **N2:** `evaluateRules` treats a full-24h slot (start 00:00, end next-day 00:00) as fittable under a
  `[0,1440]` window. Semantically defensible (exclusive end sentinel) but unusual commercially; consider
  a max-duration policy knob if merchants ever object. Not a defect against any binding document.
- **N3:** Domain suites currently run through the platform-owned vitest config. Works today because the
  glob covers `tests/**`; if the integration lane ever narrows that glob, rules coverage silently
  disappears. Director may want an explicit ownership note in the next cycle plan.

## 6. Verdict rationale

Every blocking finding of `CYCLE_32692407760_RULES.md` (B1–B4) is verifiably repaired, all five
opportunistic non-blocking findings (A1–A5) were addressed in the same repair, scope discipline is
exact (additions only, correct lane paths, canonical contracts untouched), and every executable check —
strict typecheck, purity gate, full deterministic suite, plus my own independent adversarial probes
including the audit's original failure repro — is green and repeatable. The lane is safe to integrate.

VERDICT: ACCEPT
