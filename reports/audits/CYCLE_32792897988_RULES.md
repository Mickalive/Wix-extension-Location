# Lane Audit — RULES-C3-1 (cycle 3, run 32792897988)

- **Auditor:** independent lane auditor (rules lane)
- **Candidate:** `b8be582` ("Wix build 32792897988: rules candidate (active)"), worktree `/tmp/wix_rules_candidate`
- **Accepted base:** `108b9ae` (current checkout, untouched)
- **Assigned task:** `docs/NEXT_CYCLE.json` → `lanes.rules` (`RULES-C3-1`): discharge the Director-tracked cross-lane validator parity obligation (dashboard audit F-N1, deferred until Rules ACCEPT — precondition met by `reports/audits/CYCLE_32787032785_RULES.md`, `VERDICT: ACCEPT`).
- **Binding documents applied:** `MAIN_PROMPT.md`, `docs/WIX_TECHNICAL_CONTRACT.md`, `docs/BUILD_BLUEPRINT.md`, `directives/RULES.md`, `docs/NEXT_CYCLE.json` (incl. `canonical_contracts_notice` and `vitest_glob_rule`).

## 1. Real diff inspection

`git diff --name-only 108b9ae..b8be582` — exactly two files, +804/−0:

| Path | Change | In sanctioned scope? |
|---|---|---|
| `tests/domain/uiValidatorParity.spec.ts` | new, 793 lines | yes (`tests/domain/**`) |
| `src/domain/README.md` | +11 lines, ownership note only | yes (explicitly allowed note) |

- `src/domain/ports.ts`: **byte-for-byte unchanged** (SHA-256 `af68e698…fbc` identical in base and candidate). The `canonical_contracts_notice` is honored.
- No changes to `src/ui/**` (dashboard-owned), no shared/** edits, no governance files touched, no feature code of any kind. Zero unrelated scope. No existing suite was modified or weakened (256 baseline tests intact; suite grew 256 → 286 = +30, all from the new spec).

## 2. Task-fidelity verification

The spec imports BOTH required validators:
- canonical `validateRuleSet` (+ `DEFAULT_COUNT_INCLUDED_STATUSES`, `weekdayOfDate`) from the `src/domain` barrel;
- dashboard provisional `validateRuleDraft` from `src/ui/validation/ruleDraftValidators.js` via its documented entry point (`src/ui/README.md` decision-of-record item 1). This cross-import is exactly what the task mandates; the imported module is pure (no Wix imports, no I/O — verified by reading it), so domain purity is preserved.

Six mandated corpus families are all present and asserted on both sides with exact pinned codes:
1. fully valid draft → `F1-valid-full-draft` (split hours, CLOSED + OVERRIDE-with-hours, DAY/SERVICE/LOCATION caps; zero issues both sides);
2. incomplete/invalid time rows → `F2a–F2e` (empty row, half-empty row, malformed times, inverted window, zero-length window);
3. unknown weekday keys → `F3-unknown-weekday-bucket`;
4. non-integer/negative caps → `F4a` (1.5), `F4b` (−3), plus `F4c` (0) as declared divergence R1;
5. overlapping duplicate windows (same scope+weekday) → `F5a` partial overlap, `F5b` exact duplicates, `F5c` adjacent-window control;
6. exception kind transitions → `F6-t0…t6` covering CLOSED→OVERRIDE-with-hours, OVERRIDE hours removed, removal back to CLOSED, stale-windows-on-CLOSED control, inverted/malformed override hours.

Ledger extras (`X1–X5`) complete both code universes, and a ledger-integrity test proves every declared classification and every mapping target is exercised (no dead ledger entries). A total classifier (`classifyUiCode`) makes any future unclassified UI code fail the suite instead of rotting silently.

## 3. Executable checks (all run by this auditor in the candidate worktree)

| Check | Command | Result |
|---|---|---|
| Strict typecheck | `npx tsc --noEmit` (strict, `noUncheckedIndexedAccess`) | exit 0 |
| Purity gate | `npm run check:purity` | exit 0 |
| Full deterministic suite | `npx vitest run --config src/platform/vitest.config.ts` | **29 files / 286 tests passed, 0 skipped/failed** (incl. `uiValidatorParity.spec.ts` 30/30) |
| Composite gate | `npm run check` (= typecheck + purity + vitest) | exit 0 |
| Deselection hygiene | grep `.skip/.todo/.only/xit/xdescribe/fit/fdescribe` over `tests/` | none found |
| Canonical contract integrity | SHA-256 `ports.ts` base vs candidate | identical |

Note: the mid-run "PURITY GATE FAILED" text during vitest is output of `tests/platform/purity-gate.spec.ts`, a negative test that deliberately builds a violating fixture and asserts the gate detects it. That test passes; the standalone gate is green.

## 4. Adversarial verification — parity assertions proven live (mutation testing)

To falsify vacuity I ran four mutations on a throwaway copy (candidate worktree untouched; copy deleted afterwards):

1. **Weaken UI validator** (drop `LIMIT_NEGATIVE` emission) → suite fails (pinned corpus entry F4b + ledger-integrity test).
2. **Weaken domain validator** (`maxCount < 1` → `< 0`) → suite fails (F4c pin + the R1 divergence assertion itself).
3. **Inject an unclassified UI code** (`WINDOW_EXTRA_MYSTERY`) → suite fails via exact pinning and ledger totality.
4. **Ledger rot** (delete `WINDOW_OVERLAP` from `UI_ONLY_CODES`) → suite fails with the classifier's actionable "extend the parity ledger consciously" error.

The suite therefore genuinely pins both validators and cannot pass if either side drifts or the ledger goes stale.

I also independently confirmed all four declared divergences are real semantic differences (by reading both unchanged validator sources), not fabrications:
- **R1:** UI `validateLimit` accepts integer 0 (`rawValue < 0` is its only sign check); canonical validator requires `maxCount ≥ 1` → `INVALID_VALUE`.
- **R2:** UI emits `WINDOW_OVERLAP` per intersecting pair; `validateWindowList` has no overlap check (evaluation unions windows).
- **R3:** UI flags `EXCEPTION_DUPLICATE_DATE`; canonical validation checks unique ids only, so distinct-id same-date exceptions are valid.
- **R4:** UI checks scope existence against a catalog input; `validateRuleSet(rules)` has no catalog parameter.

Each finding is documented inside the spec with exact inputs, both outcomes asserted live, and an explicit disposition note — satisfying the acceptance criterion "reported as a concrete finding … never silently reconciled". Neither side was weakened to force agreement (mutation 2 proves the pins bite).

## 5. Acceptance criteria scorecard

| # | Criterion (RULES-C3-1) | Result |
|---|---|---|
| 1 | Parity spec runs under repo vitest config, green, six corpus families asserted on both sides | **PASS** (30/30 within 286-test suite; families enumerated §2) |
| 2 | No .skip/.todo/.only; zero weakening of existing suites; strict tsc clean; purity gate green | **PASS** (§3) |
| 3 | Mismatches reported as concrete findings with exact inputs and both outcomes | **PASS** (R1–R4, §4) |
| 4 | Scope limited to tests/domain/** + src/domain/README.md note; ports.ts byte-for-byte unchanged | **PASS** (§1) |
| 5 | Fresh independent lane audit ends VERDICT: ACCEPT | **This audit** |

Directive compliance (`directives/RULES.md`): no Wix SDK import anywhere near the domain core (gate-enforced); no I/O; no new rule features; the README ownership note matches audit N3 and the NEXT_CYCLE `vitest_glob_rule` verbatim in substance (glob `tests/**/*.spec.ts` verified in `src/platform/vitest.config.ts` line 16).

## 6. Findings for Director disposition (not rules-lane defects)

The four declared divergences R1–R4 are cross-lane evidence for the DASH side of the F-N1 repoint and must not be lost at integration:

- **R1 (zero cap):** before any mirror repoint, decide whether the editor forbids 0 (canonical floor is 1) or the seam defines the 0 translation explicitly.
- **R2 (window overlap):** decide whether overlap checking survives the repoint as a UI-only advisory check or is dropped; it must not silently disappear.
- **R3 (duplicate exception dates):** keep as documented UI simplification or align semantics.
- **R4 (unknown scopes):** catalog-existence checks must move OUTSIDE the canonical validator into seam responsibility at repoint time.

Additionally, converter-local knowledge in the spec (`weekdayForExceptionDate` synthesizing a weekday for override windows, fixed fallback for invalid dates; empty/null draft caps omitted rather than stored) is correctly self-documented as contract knowledge a future mirror repoint must preserve.

## 7. Non-blocking observations

- **N1:** When DASH-C3-1's server-shaped injection seam lands and the eventual repoint executes, this ledger must be consciously updated (it will fail loudly otherwise — by design). Director should reference this spec in the repoint task.
- **N2:** The parity corpus uses fixed calendar dates without DST-boundary fixtures; DST behavior is out of this task's six mandated families and remains covered by existing domain suites (`tests/domain/time/**`). No action needed this cycle.

## 8. Verdict rationale

The candidate does exactly and only what RULES-C3-1 assigns: a rigorous, provably-live dual-validator parity contract with honest, pinned divergence documentation, plus the mandated ownership note. Every executable check is green and repeatable, canonical contracts are untouched, scope discipline is exact, and my own adversarial mutations confirm the suite cannot silently rot. Nothing blocks integration in this lane.

VERDICT: ACCEPT
