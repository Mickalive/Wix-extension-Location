# Lane Audit — Rules Candidate, Cycle 4 (run 32881643441)

- **Auditor:** independent lane-auditor (`rules-engine-builder` candidate)
- **Candidate worktree:** `/tmp/wix_rules_candidate` (single commit `9117d15` on accepted base `adb0b23`)
- **Accepted base:** current checkout HEAD `adb0b23` ("Wix build 32792897988: director attempt"), untouched
- **Assigned task:** `RULES-C4-1` from `docs/NEXT_CYCLE.json` (cycle 4): target-aware evaluation semantics (Integration audit Observation A) inside `src/domain/**` + `tests/domain/**`
- **Binding references:** `MAIN_PROMPT.md`, `docs/WIX_TECHNICAL_CONTRACT.md` (§5.3, §7, §10 #9, §12, §15), `docs/BUILD_BLUEPRINT.md` (§5, §6), `directives/RULES.md`, cycle-4 `canonical_contracts_notice`, `.opencode/job-descriptions/lane-auditor.md`

---

## 1. Real diff inspection

Diff `adb0b23..9117d15`: 5 files, +938/−30. Path filter confirms **every changed file is under `src/domain/**` or `tests/domain/**`** — exact task scope, zero cross-lane edits:

- `src/domain/ports.ts` (+43, **0 deleted lines**) — new `EvaluationTarget` type aliasing the canonical shared `TargetOperation` union (compile-time sync with `failureSemanticsFor`); new `EvaluationTargetContext { target; subjectBookingId? }`; ONE new OPTIONAL field `targetContext?` on `EvaluationDeps`. Mechanically verified strictly additive per the cycle-4 `canonical_contracts_notice`.
- `src/domain/evaluate.ts` (+120/−30) — reads `deps.targetContext ?? DEFAULT_TARGET_CONTEXT` (`{target:'CREATE'}`); entitlement stage wrapped in `if (target !== 'CANCEL')`; slot-shape classification runs for EVERY target (§5.3 fail-closed preserved); CANCEL takes a documented no-op branch falling through to the explicit allow; CREATE/RESCHEDULE share stages 2–4 against the PROPOSED slot; duplicates receive `excludeBookingId` only for RESCHEDULE (`null` otherwise).
- `src/domain/duplicates/duplicates.ts` (+22, **0 deleted lines**) — optional `excludeBookingId` on `DuplicateProposal`; conservative skip requiring an EXACT id match on a fact that carries an id; id-less facts can never be excluded. Reuses the pre-existing `bookingId?: string` on `ExistingBookingFact` (verified present in accepted base).
- `src/domain/README.md` (+119) — per-target rule-family matrix with per-cell rationale citing Contract §5.3; three honest residuals; dev-site gate implications for T-VP1–T-VP5; ports-evolution record as mandated.
- `tests/domain/targets/targetAware.spec.ts` (+664, new) — Part 1: 12-scenario default-contract pin (deep-equal outcomes incl. customerMessage strings) + corpus-size pin; Part 2: both Observation-A probes, mandated controls, matrix behaviors.

Canonical contracts respected: `src/shared/**` diff is EMPTY; `src/platform/**`, `src/billing/**`, `src/ui/**` untouched; grep proves no platform/billing/ui/shared file references `targetContext`/`EvaluationTarget` (consumers unforked); `package.json`, `tsconfig.json`, vitest config untouched (glob rule intact). No secrets, no account-specific identifiers, no fabricated UUIDs (grepped the full diff).

## 2. Executed checks (all run by this auditor in the candidate worktree)

| Check | Result |
|---|---|
| `npm ci` | green (47 packages, credential-free) |
| `npm run check` (tsc strict + purity gate + vitest) | **exit 0**, 39 files / **423 passed** |
| `npm run check:offline` (proxies pinned to 127.0.0.1:9) | **exit 0**, 423 passed |
| `npx tsc --noEmit` standalone | exit 0 (platform/billing consumers compile untouched) |
| Accepted-base baseline suite (fresh worktree at `adb0b23`) | 38 files / **392 passed** — delta is exactly the 31 new tests; zero existing tests modified |
| Skip/todo/only scan over `tests/` + `src/` | none |
| `@wix/` import scan under `src/domain/**`, `src/billing/pure/**` | none (single doc-comment mention of the ban itself) |
| Deleted-line counts, `ports.ts` / `duplicates.ts` | 0 / 0 → strictly additive |

Note: the "PURITY GATE FAILED" console line during the suite remains the known positive-control fixture in `purity-gate.spec.ts`; the suite exits 0.

## 3. Independent verification of the bit-for-bit CREATE-preservation claim

The candidate claims Part 1 was executed green against the unmodified cycle-3 tree before landing. I did not take that on faith: I checked out the accepted base into an isolated worktree, copied ONLY the new spec into it, and ran it against the unmodified base evaluator:

- **All 13 Part-1 default-dep pins PASS on the unmodified base** — the safe default is empirically identical to pre-change outcomes across the sampled corpus (allow/outside-hours/closed-exception/quota/duplicate/identity-conflict/invalid-slot/ruleset-invalid/degraded-entitlement/uncovered-location/violation-accumulation/count-unavailable + corpus-size pin).
- **Exactly the 8 genuinely-new target-aware assertions FAIL on base** (CANCEL family skipping ×5, RESCHEDULE subject exclusion ×2, identity-cross-service-with-exclusion ×1) while the 10 assertions expressing unchanged legacy semantics (defect baseline, third-party-overlap control, mismatched-id control, conservative no-id exclusion, proposed-slot caps/windows) PASS on base — precisely the expected partition. The claim is truthful and the regression net is real.

## 4. Acceptance-criteria verification

1. **Observation-A probes as regressions + controls** — VERIFIED. Probe 1 (cancel of the only booking on an at-capacity day ⇒ allow, no `QUOTA_EXCEEDED`/`DUPLICATE_BOOKING`) with a defect-baseline control proving identical inputs WITHOUT context still block (audit reproduction pinned forever); zero-count-query proof that the cap family is skipped, not merely satisfied. Probe 2 (reschedule overlapping own still-existing booking ⇒ no `DUPLICATE_BOOKING`) with the mandated second-conflicting-booking clause, a genuine third-party-overlap control, conservative no-id and mismatched-id exclusions, and identity-keyed cross-service conflicts still firing.
2. **CREATE unchanged** — VERIFIED beyond the claimed evidence (§3): full pre-existing suite green unmodified on both trees; default-deep-equal pin corpus green on the unmodified base; `excludeBookingId: null` provably inert through the guard (`undefined`/`null` both fail the skip conditions — identical to the previous absent field).
3. **Strictly additive ports evolution** — VERIFIED mechanically (0 deleted lines) and by repo-wide strict tsc; consumers reference nothing new.
4. **Matrix documented citing §5.3** — VERIFIED in README: classification everywhere (fail-closed preserved); CANCEL skips entitlement/windows/exceptions/caps/duplicates with per-cell rationale; RESCHEDULE evaluates availability families against the PROPOSED slot with subject-excluded duplicates. Includes the mandated dev-site gate implications (cancel-frees-capacity probe added to T-VP set; RESCHEDULE identity inputs folded into the T-VP3 payload probe; surface-coverage claims stay banned; fail-open reschedule posture restated).
5. **Purity/no-skip** — VERIFIED (§2 table).
6. **This audit** — ends ACCEPT below.

## 5. Adversarial probes executed by this auditor

Ten independent probes written and run against the candidate (temporary spec, removed afterwards; worktree left clean):

1. Explicit `subjectBookingId: null` on RESCHEDULE ⇒ exclusion inert, own-booking overlap still blocks — matches disclosed residual 2 exactly.
2. Absent `subjectBookingId` on RESCHEDULE ⇒ same (residual honored, never hidden).
3. Runtime-unknown target value (`'DELETE'` cast past typing) ⇒ degrades to full CREATE semantics — the defense-in-depth claim is accurate.
4. CANCEL with a counter that would THROW ⇒ zero invocations, clean allow.
5. CANCEL under degraded billing signals ⇒ plain `BOOKING_ALLOWED`, zero notices (family skipped, not satisfied).
6. Cross-day RESCHEDULE ⇒ cap query bounds ONLY the proposed site-zone day (verified UTC bound `2026-08-12T04:00:00.000Z`).
7. CANCEL with a throwing duplicate-snapshot store ⇒ store never read, clean allow.
8. Subject fact carrying the same identity key but excluded by id ⇒ no false `IDENTITY_TIME_CONFLICT` from its own fact.
9. Split-window gap CANCEL ⇒ allowed despite falling between windows.
10. RESCHEDULE onto a CLOSED exception date ⇒ still blocked (`DATE_CLOSED`) — proposed-slot exception semantics hold.

Two initial probe failures were MY fixture errors (default helper booking did not overlap the proposal); corrected, all 10 pass. No candidate defect surfaced.

## 6. Findings

### Blocking findings
None.

### Non-blocking observations (do not gate)
- **A.** The CANCEL path resolves via fall-through to the shared "no explanations ⇒ allow" tail rather than an explicit return. Correct today and well-commented; if a future cycle adds a notice-emitting family it must update the CANCEL branch consciously. The matrix documentation and pin tests make silent drift loud.
- **B.** Residual 1 (RESCHEDULE same-day self-count in authoritative caps) is honestly disclosed and correctly routed to a Director-coordinated platform-side count adjustment instead of a domain guess — the right call under C1/C6 discipline; tracked here so it is not lost before T-VP evidence.
- **C.** `EvaluationTarget` aliasing the shared union means any future shared-taxonomy change surfaces as a domain compile error rather than semantic drift — good, keep it.

## 7. Verdict rationale

The candidate delivers exactly RULES-C4-1 and nothing else: a strictly additive, safely-defaulted target context that resolves both reproduced Observation-A defects at the canonical layer where they belong; binding §5.3 failure semantics strengthened (classification now explicitly runs for every target); cancel-frees-capacity and self-exclusion semantics with mandated controls; a truthful, independently re-verified bit-for-bit CREATE preservation story; exhaustive honest documentation including unproven-payload residuals and gate implications; 31 new deterministic tests on a fully green 423-test offline suite; exact scope discipline with zero cross-lane edits. My own adversarial probing found no defect. Nothing blocks integration in this lane.

VERDICT: ACCEPT
