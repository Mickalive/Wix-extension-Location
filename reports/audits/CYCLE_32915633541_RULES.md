# Lane Audit — RULES-C5-1 (cycle 6 candidate, run 32915633541)

- **Candidate:** `62800955adc7e96c29c3bb6dfa2cf70603bc451d` (mounted at `/tmp/wix_rules_candidate`, single commit "Wix build 32915633541: rules candidate (active)")
- **Accepted base:** `fd480aadb3e9b980492f5ccd2a1c3f5efe7926dd` (current checkout, byte-verified)
- **Task:** `docs/NEXT_CYCLE.json` → `lanes.rules` = RULES-C5-1, status `active`
- **Directive:** `directives/RULES.md`; contracts: `docs/WIX_TECHNICAL_CONTRACT.md`, `docs/BUILD_BLUEPRINT.md`
- **Audit method:** real diff inspection, full deterministic gate execution in the candidate worktree, baseline re-run on the accepted base, and five independent adversarial mutation probes against the new guards.

---

## 1. Diff vs accepted base (exact, complete)

```
src/domain/README.md                          |  29 +
tests/domain/evaluate.spec.ts                 | 407 +++++++ (2 deletions = import-line reorganization only)
tests/domain/helpers/targetScenarios.ts       |  64 + (new)
tests/domain/targets/matrixProperties.spec.ts | 752 + (new)
4 files changed, 1250 insertions(+), 2 deletions(-)
```

Scope verification:
- Every touched path is inside the rules lane's owned surface (`src/domain/**`, `tests/domain/**`). No platform, billing, dashboard, shared, governance, or workflow files touched.
- `src/domain/ports.ts` is byte-identical to the frozen cycle-5 SHA: independently computed `sha256sum` = `d46e0743fa825315a80456962d0f4412c02cbd437f0acabce909356f43c18802` — exactly the value pinned by `canonical_contracts_notice`.
- The 2 deleted lines in `evaluate.spec.ts` are the old two-line import block replaced by an expanded import block; no pre-existing test body or assertion was modified.
- No `.skip/.todo/.only/fit(/xit(` markers anywhere in the candidate tree (`grep -rnE` over `tests/` and `src/`: zero matches).
- No new rule features, no platform wiring, no UI validators — matches the task's `out_of_scope`.

## 2. Acceptance criteria — verified one by one

| Criterion | Evidence |
|---|---|
| `npm ci && npm run check` passes offline credential-free; purity gate green over all six roots | Executed `npm ci` (47 packages) then `npm run check:offline` (typecheck → purity → vitest) in the candidate worktree: exit code 0. Purity gate output: green over `src/domain`, `src/billing/pure`, `src/platform/http`, `src/platform/webhooks`, `src/platform/validation-plugin`, `src/platform/composition`. (The "PURITY GATE FAILED" text visible inside the vitest log is stdout of `tests/platform/purity-gate.spec.ts` exercising the gate against synthetic `/tmp/purity-gate-fixture-*` violations — expected negative testing, not a failure.) |
| Determinism sweep green across all three targets incl. DST fixtures and split-window scenarios | New test "target-matrix sweep" in `evaluate.spec.ts`: 18-scenario corpus (split windows incl. gap-blocked and midnight-end fit; DST spring-forward span/gap-start on 2026-03-08; fall-back ambiguous/narrow-window on 2026-11-01; caps, exceptions, duplicates incl. RESCHEDULE subject-exclusion, entitlement, classification) × {CREATE, CANCEL, RESCHEDULE} × 101 repetitions with freshly built deps per evaluation, byte-compared via JSON. Green; corpus-size floor (`≥15`) prevents silent shrinkage. |
| Explanation-completeness green across all three targets with customer-safe message assertions | New "matrix sweep" completeness test: ≥15 scenarios × 3 targets; asserts closed-vocabulary `ruleId` ∈ `ENGINE_RULE_IDS`, non-empty `{ruleId, code, customerMessage}`, prose shape (contains space, ends with `.`), no internal fixture-id substrings (`svc-`, `loc-`, `lim-`, `exc-`, `bk-`, `ruleset-`, `person-`, `rev-`), no machine-code substrings, no `/[A-Z]{3,}_[A-Z_]+/` pattern; plus per-target allow AND block population floors (non-vacuity). Green. |
| CANCEL-tail guard fails on synthetic non-classification injection into CANCEL; passes on real code | In-suite: `forbiddenCancelInjections` derives 10 forbidden (family,code) pairs from the README CANCEL column and proves the guard rejects every one injected as both block and allow decision; plus an end-to-end drift-mode simulation wrapping the real evaluator. Independently confirmed by mutation M1 below. |
| Matrix-consistency ties README cells to observed behavior; deliberate doc-drift simulation fails the suite | `matrixProperties.spec.ts` parses the README matrix table at collection time (loud structural errors), runs 6 behavioral probes (one characteristic outcome code per family; exceptionsWindows probed twice) under all three targets, compares observed vs documented cells, meta-pins the accepted cycle-4 cell values outright, and includes an in-memory flip simulation proving the harness detects drift in both directions. Independently confirmed by mutation M2 below. |
| ports.ts SHA preserved; no Wix imports; purity green; no skip markers; existing 465-test suite green unmodified | SHA verified (§1); domain core has zero `@wix/` imports (only a prose comment inside the frozen ports.ts); purity gate green; no skip markers; accepted base re-run: **465/465 green**; candidate: **476/476 green** (= 465 + 2 new evaluate tests + 9 matrixProperties tests). |

## 3. Adversarial mutation probes (all against a scratch clone; mounted candidate untouched)

| # | Mutation applied to REAL engine/docs | Expected guard | Result |
|---|---|---|---|
| M1 | Unconditional fail-open notice appended on the shared allow tail — the exact "future notice-emitting family forgets the CANCEL branch" mode from cycle-4 audit observation A | CANCEL-tail guard | **Tripped**: 3 matrixProperties tests failed (`non-classification ruleId 'limits' (COUNT_UNAVAILABLE_FAIL_OPEN)`), incl. battery, both anti-vacuity proofs |
| M2 | README matrix cell flipped: Caps/CANCEL `**no**` → `yes` | Consistency + meta-pin + drift simulation + derived-injection set | **Tripped**: 4 tests failed, flagging exactly `caps/CANCEL` |
| M3 | One comment line appended to frozen `ports.ts` | SHA pin | **Tripped**: freeze-pin test failed |
| M4b | Hidden call-count dependence (>5000th evaluation emits extra explanation — threshold beyond reach of every other test) | Target-matrix determinism sweep ONLY | **Tripped**: the new sweep alone caught it (17 other tests stayed green) — proving the property adds real power over the pre-existing suite |
| M5 | Customer message poisoned with `svc-1` and `(LOCATION_NOT_COVERED)` | Cross-target completeness sweep | **Tripped**: matrix completeness test failed |

All guards are live, derived from ratified artifacts, and resistant to silent disarm (meta-pins + corpus-size floors + SHA pin).

## 4. Correctness spot-checks

- **DST fixtures are real:** 2026-03-08 and 2026-11-01 verified as Sundays (US transitions); fixture comments' UTC correspondences (EST=UTC−5, EDT=UTC−4; 02:00–02:59 nonexistent spring side; 01:00–01:59 doubled fall side) match reality. Sweeps assert determinism/completeness only, so no hand-math can leak into assertions; zone math itself remains pinned by the pre-existing green `wallClock`/`localDate` suites.
- **No host-timezone leakage:** all fixtures construct instants via pure domain helpers with explicit `America/New_York`; `EvaluationDeps` carries no clock.
- **README addition is honest:** describes only executable guarantees, cites the correct test files, discloses the frozen-SHA pin, and makes no production-capability claims (T-VP gating language preserved).
- **Parser robustness:** `parseCell` throws loudly on unknown cell formats; `parseMatrix` enforces exact row count/family uniqueness; collection-time failure mode is explicit rather than obscure.

## 5. Non-blocking observations (no action required this cycle)

- O1: Matrix-consistency probes verify one characteristic code per family per target; a family emitting an *additional* unexpected code under some target is bounded only indirectly (closed-vocabulary completeness sweep + CANCEL-tail guard + meta-pins). Adequate for RULES-C5-1's scope; future families should extend PROBES consciously.
- O2: `withFlippedCell` uses first-occurrence string replacement; exercised only against known cell formats and would throw loudly on structural change. Cosmetic.
- O3: Test-side `node:fs`/`node:crypto` usage in `matrixProperties.spec.ts` is documentation-as-fixture reading; no repo writes (drift simulation operates purely in memory). Domain-core purity unaffected.

## 6. Verdict rationale

The candidate executes exactly the assigned RULES-C5-1 slice: determinism and explanation-completeness properties now sweep the full CREATE/CANCEL/RESCHEDULE matrix including DST and split-window hardness; the CANCEL fall-through tail is protected by a derived, anti-vacuity-guarded, meta-pinned executable invariant; README↔code drift now fails the suite in both directions; the frozen ports contract is pinned in-suite. All deterministic checks pass offline and credential-free; the existing 465-test suite is untouched and green; scope boundaries and purity constraints hold. Five independent mutations confirm the new guards detect precisely the drift classes they claim to detect. No blocking finding was found.

VERDICT: ACCEPT
